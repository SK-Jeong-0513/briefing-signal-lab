# 세션 핸드오프

세션을 마칠 때 아래를 채우고 커밋하세요. 다음 세션(또는 사람)이 그대로 이어받습니다.

---

## 현재 상태 (최근 세션이 갱신)

- **마지막 갱신:** 2026-08-27
- **★ 관리자 콘솔의 일일 발송 시각 설정이 무효였다 — 시트가 "07:20" 을 Date 로 저장(2026-08-27, 위 배포 중 발견):** `applyDailySchedule()` 실행 로그에 `[설정] daily_send_time 형식 오류('Sat Dec 30 1899 07:20:00 GMT+0827') → 기본값 07:40` 이 떴다. 원인은 `admin/Code.gs` `_setSetting_` 의 `setValue("07:20")` — **Google Sheets 가 이를 시각으로 자동 해석**해 1899-12-30 기준 Date 로 저장한다. 읽는 쪽(`marketSetting_`·`_getSetting_`)이 `String(cell)` 하면 저 긴 문자열이 되어 `dailySendTime_()` 의 `^(\d{1,2}):(\d{2})$` 에 걸리고 `CFG.DAILY_SEND_TIME` 으로 폴백했다. **결과: 07:20 으로 설정했는데 실제 발송은 07:40 이었다**(2026-08-27 메일 07:41:31 KST 발송이 그 증거). 즉 '일일 발송 시각 설정화' 기능은 **저장은 되는데 반영이 안 되는** 상태로 계속 있었고, 매일 03:00 `syncDailySchedule` 이 같은 오류를 로그에만 남기고 있었다. **조치:** 읽는 쪽에 `settingText_()`(메일러)·`_settingText_()`(콘솔) 추가 — 셀이 Date 면 `getHours/getMinutes` 로 `HH:MM` 을 만든다(스크립트 시간대 기준이라 시트에 보이는 값과 같다). **쓰는 쪽을 고치지 않은 이유:** 시트는 손으로도 편집되므로 읽는 쪽이 관대한 게 근본이고, 이미 저장된 Date 값도 그대로 살아난다. **영향 범위는 이 설정 하나** — `pipeline_enabled`(`'1'`/`'0'`)·`llm_primary` 는 Sheets 가 시각으로 오인할 값이 아니다. **⚠️ settings 탭에 날짜가 의미 있는 설정을 추가하면 `settingText_` 부터 고칠 것**(지금은 Date 를 무조건 시각으로 본다). **검증:** `test_daily_schedule.js` 에 케이스 추가 — Date 셀 → `07:20`, 트리거가 07:20/06:30 에 생성, 한 자리 시 0 패딩, 문자열·숫자·null 회귀, 콘솔 `_getSetting_` 동일 변환, 양쪽 변환 규칙 동기. **⚠️ Date 는 vm 컨텍스트 안에서 만들 것** — 밖에서 만들면 realm 이 달라 `instanceof Date` 가 false 다. JS 17스위트·Python 78건 통과. **⚠️ 배포:** 메일러는 51행~끝 재붙여넣기 후 `applyDailySchedule()` 재실행(07:20/06:30 으로 이동). 콘솔(`admin/Code.gs`)은 **표시 전용** 수정이라 급하지 않으나, 안 하면 관리 화면에 긴 날짜 문자열이 그대로 보인다(붙여넣기 + 같은 URL 유지 재배포 필요).
- **★★ 일일 메일 '주요 시장 지표' 블록 재차 누락 → 사전 점검·알림 도입(2026-08-27):** 08-27 메일에서 블록이 통째로 빠졌다(구독자 제보, 두 번째). **원인은 코드가 아니라 GitHub 예약 크론 지연이다.** `daily-quotes.yml`의 08-26 21:10 UTC 예약분이 **3시간 29분 늦은 08-27 00:39 UTC**에야 돌았고(run `33027574576`), 그 사이 07:41 KST 발송이 나갔다. 발송 시점 `quotes.json`은 `briefing_date=2026-08-26`이라 `quotes_()`의 당일 게이트가 블록을 생략했다 — **게이트는 의도대로 동작했다**(전일 종가를 오늘 것처럼 내보내는 대신 공백). `git log -- public/assets/data/quotes.json`에서 08-09~08-25 매일 있던 커밋이 **08-26만 없다.** 같은 UTC 일자에 `dashboard-data`(21:30)·`market-data`(22:30)도 실행 이력이 0이라 **daily-quotes만의 문제가 아니라 그날 예약분 전체가 밀린 것**이다. **이 위험은 이미 아래 2026-08-03 항목에 '지연이 더 커지면 평일에도 같은 증상이 난다'로 적혀 있었고, 그대로 실현됐다** — 06:10 갱신 → 07:40 발송의 여유가 90분뿐인데 GitHub 예약 크론은 best-effort라 지연 보장이 없다. **조치 ①(자가 복구):** `preflightQuotes()` — 발송 50분 전(07:40 → 06:50 KST) 트리거가 `quotes.json` 신선도를 보고, 낡았으면 `workflow_dispatch`로 `daily-quotes.yml`을 **직접 발화**한 뒤 20초 × 6회까지 반영을 기다린다. **발화 주체가 Apps Script 트리거라 GitHub 스케줄러가 통째로 밀려도 산다.** 발송 경로에는 지연을 더하지 않는다(별도 트리거). 시각은 `applyDailySchedule()`이 발송 시각에서 계산하고, **06:20 KST 바닥**을 둔다 — 겨울(EST) 미 마감이 21:00 UTC=06:00 KST라 그보다 이르면 마감 전 데이터를 물어온다. **조치 ②(알림):** 블록이 실제로 빠지면 `alertQuotesMissing_()`이 운영자에게 메일. 종전에는 `Logger.log`만 남아 **두 번 다 구독자가 먼저 발견했다.** 발화 실패·미반영도 각각 알린다. **⚠️ 배포 필요:** `mailer/Code.gs` 3개 블록 붙여넣기 + 스크립트 속성 `GH_DISPATCH_TOKEN`(파인그레인드 PAT · Actions read/write) + **`applyDailySchedule()` 1회 실행**(트리거는 이 함수가 만든다). 절차·행 번호·진단 로그 표는 `mailer/README.md`. **CFG·워크플로 YAML·`fetch_dashboard.py`는 변경 없음.** **검증:** JS 17스위트·Python 78건 통과(신규 `tests/test_quotes_preflight.js` 12케이스 + `test_daily_schedule.js`에 트리거 케이스 추가). **미해결:** 사전 점검도 07:41 발송 ±15분 안에 워크플로가 끝나야 한다 — GitHub이 발화분까지 몇 분 이상 지연시키면 ②의 알림만 뜨고 블록은 여전히 빠진다. 그때는 발송 시각을 뒤로 밀거나(KRX 장전 단일가 08:30 이전이 상한) 지표를 Apps Script가 직접 수집하는 쪽을 검토한다.
- **★★ 랜딩 무료 중심 개편(2026-08-18) — ✅ 배포(`7a21142`·`73e6f23`):** 일일 시황·주간 브리핑·스페셜 리포트가 **전부 무료 구독자에게 나가는데** 랜딩은 여전히 유료 전제였고 일부는 **사실과도 달랐다** — 비교표가 '이메일 발송'을 유료 전용으로 적고 있었다. **멤버십 2열 비교표 → 무료 카드(받는 것 7가지 전부 체크) + 유료 카드(그레이 로드맵).** **강조를 뒤집었다** — primary 테두리·그림자가 유료 쪽에 있었는데 지금 실제로 쓸 수 있는 쪽은 무료다(유료는 `canvas` 배경 + 점선 + `muted`). **샘플 카드 자물쇠 배지**("전체 브리핑은 유료 구독")를 메일 아이콘 + "전체 브리핑은 무료 구독 메일로"로 교체 — 전부 무료인데 카드마다 잠금이 붙어 있으면 정반대 신호다. 히어로·하단 CTA·**메타 설명 3곳(og·twitter 포함)**도 교체(검색·SNS 공유의 첫인상). **⚠️ 유료 카드에 기능 목록을 적지 않는다** — 무엇을 유료로 할지 미정이고, 지금 무료로 주는 항목을 유료 칸에 옮기면 왼쪽 카드와 정면 모순이다(옛 비교표가 정확히 그 상태였다). 도입 시점과 사전 안내 약속만 적는다. **⚠️ 유료 CTA는 `LINKS.paidForm` 이 비면 `<a>` 가 아니라 `<span aria-disabled="true">` 로 그린다** — 옛 결함이 "유료 문의를 눌렀더니 브리핑과 무관한 컨설팅 설문이 열린다" 였으므로 갈 곳이 없으면 클릭도 탭 이동도 안 돼야 한다. URL 한 줄 넣으면 그대로 진짜 버튼이 된다(약 1년 뒤 유료 전환 시 복구 경로). 내 변경으로 고아가 된 `.plan--paid` 규칙 제거. **요금 정책은 CLAUDE.md '1. 제품 한 문장' 아래에 명문화했다.** **남은 것:** 기술·금융·경제 페이지의 잠금 문구 약 15곳은 **손대지 않았다** — 그 페이지들이 실제로 무엇을 노출할지 결정이 선행돼야 한다.
- **★★ 엔진 응답 검증(2026-08-18) — ✅ 배포(`029b958`·`90d3525`):** 어제 `gemini-2.0-flash` EOS 를 `3.5-flash` 로 바꿨더니 **더 나쁜 실패**가 나왔다. **추론 토큰을 쓰는 모델**이라 `max_tokens=400` 을 사고 과정에 써버리고 답변은 꼬리만 22~41자 남겼다 — `{"요약":"삼성전자와 SK하이닉스는 상반기 실`, `exactly 2 sentences):**`, `):** * *Draft 1:* 리노공` 같은 **자기 초안 조각이 종목 카드에 그대로 떴다**. 404(깨끗한 실패, 게이트가 잡음)를 **절단된 쓰레기(조용한 오염)**로 바꾼 것이다. **구멍이 두 층:** ① `chat()` 이 '비어 있지 않으면 성공' 으로 처리해 22자짜리가 폴백 없이 통과 → `min_chars` 인자 추가(`fetch_market` 80 · `prepare_weekly_release` 60). ② `fetch_market` 이 **JSON 파싱 실패 시 원문을 그대로 요약에 저장** — 이게 실제 오염 경로다. 파싱 실패 = 생성 실패이므로 버린다(카드는 종목별 최신 1건만 쓰므로 하루 빠져도 옛 카드가 남는다). `fetch_weekly` 는 정상적인 '해당 없음' 이 `[]` 2자라 길이로 구분 불가 → **배열 유무**를 본다. **⚠️ 어제 내 판단이 틀렸다** — 주간 초안 dry 의 '후보 0건' 을 "월요일이라 헤드라인 소진" 으로 봤는데, 로그가 **엔진별로 정확히 갈렸다**(gemini 7개 도메인 전부 0건 / deepseek 3개는 6~8건). 같은 절단 버그였고 빈 배열(정상)과 배열 없음(실패)이 같은 로그로 찍혀 안 보였다. **`ai.py --test` 가 엔진을 하나씩 친다** — 예전엔 `chat()` 한 번으로 끝나 주력이 답하면 통과라 **폴백 고장이 구조적으로 안 보였다**(gemini 404 가 몇 주 숨은 이유). 검수 게이트는 생성 엔진을 제외하므로 폴백만 남는 경로가 실제로 있다. 이 테스트로 **`gemini-3.5-flash-lite` 133자 정상 확인(2/2 OK)**. **모델 id 는 `GEMINI_MODEL` 워크플로 env**(기본 flash-lite) — EOS 는 또 오므로 코드 배포 없이 바꾼다. **`llm_primary` 는 deepseek**(운영자가 콘솔에서 변경). **⚠️ 그래서 월요일 게이트는 gemini 단독 경로다** — deepseek 이 생성하면 독립성 규칙이 deepseek 을 제외한다. `min_chars` 는 게이트를 구하지 못한다(넘어갈 다음 엔진이 없어 결국 `independent_evaluator`). 원인은 로그에 보이지만 결과는 같다. **종목 카드 중복:** 국내 코드는 6자리인데 시트가 숫자로 인식해 앞자리 0을 떨어뜨려(`005930`→`5930`) 중복 제거 키가 갈렸고 7월 카드가 8월 카드와 나란히 떴다 — 숫자 코드는 6자리로 채운다. **검증:** market-data 실행 5종목 147~240자 정상·쓰기 200. `tests/test_ai_fallback.py` 10건 신규.
- **★★ 지표 대시보드 확장(2026-08-18 grill 확정) — ✅ 배포 완료(`aee3b4c`):** 관계 페어 **6→12개**, 미 섹터 카드 **11개** 새 섹션. 요청 14개 중 3개(미 채권금리↔지수·MSCI 한국·국내 수출)는 **이미 있었고**, 4개(수급·외국인·공매도·시간대별 거래량)는 Yahoo 에 없거나 일봉 모델이 아니라 보류했다. **신규 페어:** M2↔지수(FRED `WM2NS`) · 준비금↔지수(`WRESBAL`) · 금↔비트코인 · WTI↔국채금리(`DGS2`/`DGS3`/`DGS10`) · 거래량 한국↔미국(20일평균 대비) · **시장폭↔미국지수**(`RSP/SPY`). **FRED 가 새 소스**(키 불필요). **요청받은 `M2SL`·`TOTRESNS` 를 쓰지 않았다** — 둘 다 월별 + 발표지연 **78일**이라 차트 오른쪽이 두 달 반 비었다. 같은 개념의 주간판(`WM2NS` 43일 / `WRESBAL` 6일)으로 바꿨고 교체 비용은 0(같은 CSV 경로, id 만 다름). 국채 3년물은 **Yahoo 에 지수 심볼이 아예 없다(404)** — `^TNX`/`^FVX`/`^TYX` 는 있는데 3년물이 없어 FRED 가 유일한 경로다. **⚠️ 함정 3개를 실제로 밟았다:** ① **FRED 는 `Accept` 헤더가 없으면 연결을 끊는다** — 첫 실행에서 FRED 5개가 전부 20초 타임아웃(WinError 10054)으로 빠졌다. 네트워크 문제처럼 보이는데 헤더 한 줄이면 0.2초에 200. `_get_text()` 가 `User-Agent` 만 보내고 있었다(`get()` 은 `Accept` 를 보낸다). ② **FRED 는 전체 역사를 준다** — `DGS10` 이 1962년부터 16,140행. 안 자르면 다섯 시리즈가 741KB 를 먹어 파일이 **1,134KB**. `_range_days()` 로 `RANGE` 창에 맞춰 자른다(RANGE 를 늘리면 FRED 도 따라가게). ③ **`quotes.json` 의 US2Y 가 쓰는 `2YY=F`(선물) 최근 종가가 `None` 으로 온다** — null 제거 로직 덕에 당장 안 깨지지만 품질 불안. **이번 범위 밖이라 기록만.** **설계 근거(측정으로 결정):** 거래량은 **정규화해서 담는다** — Yahoo 의 KOSPI 거래량은 천주, 미국은 주 단위로 와 약 1000배 차이가 나 원본을 겹치면 한국 선이 바닥에 붙는다(게다가 주식 '수' 는 시장 간 비교가 원래 무의미 — 한국은 저가주가 많아 주식 수가 부풀어난다). 시장폭은 **비율로 담는다** — RSP·SPY 를 좌우축에 나란히 두면 **이중축이 각자 자동 스케일링해 두 선이 포개져 발산이 안 보인다**. **VIX 와 하이일드 스프레드는 검토 후 뺐다** — S&P 20일변화 상관이 각각 **-0.78** 로 지수를 뒤집어 그린 것과 같아 교차검증이 안 된다(시장폭 -0.41, 장단기 10Y-2Y -0.31). 참고로 **레벨 상관은 착시다** — M2↔S&P 레벨 +0.94 가 20일변화로는 **-0.27**. 기존 `금리↔지수` 도 -0.13 이라 이 대시보드는 원래 강한 상관을 증명하는 도구가 아니다. **섹터는 페어가 아니라 카드** — "어느 섹터가 앞서나"는 2계열 오버레이로 못 본다(드롭다운을 11번 갈아끼워야 한다). `vcCard` 재사용이라 JS 는 ~10줄. **카드는 마지막 90개만 읽는다**(`spark()` 가 `slice(-90)`, `pctChg` 가 -22) → 섹터는 **90일치만 저장**(3년치면 663개가 안 읽히고 150KB 낭비). 기존 `valuechain` 5개도 같은 낭비 ~70KB 가 있으나 범위 밖이라 안 건드렸다. **용량 266→434KB(gzip 67→111KB)**, 저장소 이력은 델타 압축이 먹어 비문제(49커밋에 팩 723KB). **수집 실패를 보이게:** 배포는 계속하되(수집 실패로 배포를 막으면 멀쩡한 나머지 지표까지 낡는다) 흔적을 남긴다 — `SCORE 페어 N/M · 섹터 X/Y` 를 스크립트가 찍고 **워크플로가 커밋 제목에 넣으며**, `dashboard.json` 의 `coverage` 를 화면 하단이 읽어 **결손일 때만** `지표 9/12(일부 출처 지연)` 를 표시한다. 워크플로에 `set -o pipefail`(tee 가 종료코드를 삼켜 크래시가 초록불이 되는 것 방지). **이 장치가 첫 실행에서 바로 FRED 5개 누락을 잡았다.** **⚠️ 한 페어 안에서 소스를 섞지 말 것** — FRED(1~4일 지연) + Yahoo(당일) 를 같은 드롭다운에 두면 옵션을 바꿀 때마다 차트 끝 날짜가 달라진다. 기존 `tnx`(Yahoo)는 `rate-index`·`metal-rate` 가 쓰고 있어 **교체하지 않았다**(요청 범위 밖 화면을 흔들지 않는다). **검증:** 로컬 실행 — 시리즈 43 · 페어 12/12 · 섹터 11/11 · 신규 페어 전부 137~754 포인트 렌더 · i18n 키 25개 해석 · 섹터 카드 11개 렌더. `tests/test_dashboard_pairs.py` 17건 신규(Python 68 · JS 16). **푸시 중 자동 갱신 워크플로와 데이터 파일 충돌** — 신규 시리즈가 든 로컬 빌드를 채택해 해소.
- **★★ 발송 전면 중단 사고와 복구(2026-08-17) — ✅ 원인 확정·복구·재발방지 배포:** 스페셜 리포트가 `발송중 · 0건` 에 멈췄고 같은 날 일일 시황도 안 나갔다. **원인은 OAuth 스코프다.** 전날 `b633af7` 에서 `MailApp` 이 이 파일에 처음 들어왔는데 `GmailApp` 과 스코프가 다르다(`script.send_mail`). **웹앱 배포본은 재배포 시 새 스코프를 받지만 시간 트리거는 사람이 재승인하기 전까지 옛 권한으로 돈다** → "테스트 발송은 되는데 예약만 안 된다"는 비대칭. **진단 경로(추측 아님):** `주간-발송로그`에 `special:` 행이 **0건** + 발송수 0 + `message` 공란 → 첫 `sendMail_` 전 예외 → 크래시 지점이 `specialSendRow_` 세 줄로 좁혀짐 → `git log -S "MailApp"` 으로 도입 시점 확인 → 판별 질문(일일도 안 왔나 + `시장-일일`에 오늘 행 11건 존재하므로 데이터 부재 아님) → 사용자가 `checkMailerProps` 실행 시 **"승인 필요" 창**이 떠 확정. 승인 후 재시도 → **25명 발송·실패 0**. **재발 방지 2건:** ① `specialSendRow_` 를 try/catch 로 감싸 루프 밖 크래시 사유를 `message` 에 `중단: <사유>` 로 남기고 상태를 `실패` 로 둔다(폴링이 다시 안 집는 성질 유지). ② **진단용 호출이 발송을 죽이지 못하게** — 스페셜의 한도 조회는 경고 로그 전용인데도 전체를 끊고 있었고, `mailQuotaOk_` 도 조회 실패 시 통과시킨다(그 함수 자신의 fail-open 원칙 그대로). **⚠️ 콘솔 화면은 자동 갱신되지 않는다** — 재시도 후 폴링이 발송을 끝내도 화면은 `대기 · 0` 인 채라 새로고침해야 한다. 낡은 화면에서 눌러도 안전(`specialCancel` 이 클릭 시점에 시트를 다시 읽어 거부).
- **★★ 주간 발행 부실의 3중 원인(2026-08-17) — ✅ 규명·2건 수정, 1건 미수정:** W34 가 18건만 발행되고 화면엔 8건만 보인 원인을 데이터로 규명했다. 초안은 **121건 전부 `approved`** 였다. **①(운영) 04:00 자동 게이트는 `status=approved` 를 읽지 않는다** — `select_candidates` 가 `!= 'rejected'` 만 거르고 7게이트를 다시 돌린다. 운영자 승인을 반영하려면 **04:00 전에 콘솔 '이번 호 발행 예약'** 을 눌러 `manual_ready` 원장을 만들어야 하고 그러면 게이트가 no-op 한다(W31·W32·W33 이 그 경로: 48·40·13건). **②(사고) 탈락 103건 중 80건이 품질 미달이 아니라 검수 LLM 404** — 원장 `제외 103건 (evaluation_gate 23, independent_evaluator 80)`. Actions 로그 실측: gemini 호출 **121건 전부 HTTP 404** `"models/gemini-2.0-flash is no longer available"`. **모델이 퇴역했다.** settings 의 `llm_primary=gemini` 라 모든 호출이 gemini 를 먼저 친다. **숫자가 정확히 맞는다** — 생성엔진이 `deepseek` 인 80건은 독립검수 규칙으로 deepseek 이 제외돼 gemini 가 유일 후보라 전량 탈락, `telegrampick/...` 41건은 그 문자열이 `ENGINES` 이름과 달라 아무것도 제외되지 않아 deepseek 이 검수(41 = 통과 18 + evaluation_gate 23). **원문제목·출처URL 결손은 0건** — 어제의 원문제목 보강은 효과가 있었고 병목이 검수 엔진으로 옮겨갔다. **→ `gemini-3.5-flash` 로 교체(`81de5c8`)** — TelegramPick 이 2026-08-10 같은 EOS 를 겪고 v1beta/models 로 확인해 옮긴 목록에 있는 id. 검증: dry 실행에서 11개 도메인 전부 통과, 404 0건(503 2건은 deepseek 폴백 정상). **⚠️ `ai.py --test` 는 이 고장을 못 잡는다** — `exclude_engines` 없이 돌아 deepseek 이 먼저 답한다. **⚠️ 엔진이 2개뿐이라 하나가 죽으면 그 엔진이 만든 항목은 독립검수가 원천적으로 불가능하다.** **③(미수정) 통과한 18건이 전부 headliner 라 8건만 노출** — 사이트 `script.js:903`(`if (!bucket.headliner)`)과 메일러 `weeklyReleaseCats_` 가 **분야당 첫 headliner 하나만 채택하고 나머지를 버린다.** W33 도 13건 전부 headliner 라 5건만 나왔다. **일괄 headliner 지정이 오히려 콘텐츠를 지운다.** W34 는 사용자가 headliner 10 · signal 111 로 정리해 rev.2 로 **121건 전량 노출**. 코드 수정(2번째 이후 headliner 를 signal 로 강등)은 **미착수**. **부수:** gemini 교체가 잠재 크래시를 드러냈다 — `fetch_weekly.py` 가 배열 안 int 로 `AttributeError` 사망, 도메인 하나가 죽으면 `main()` 루프가 끊겨 **그 주 수집을 통째로 잃는** 구조라 `_parse_cards` 에서 dict 만 남기게 고쳤다(`97da54c`).
- **★★ 모바일 세로 화면 2건(2026-08-17) — ✅ 배포(`4caaf15`):** ① 620px 이하에서 `.nav{display:none}` 이라 헤더에 브랜드·언어·구독만 남아 **기술·경제·금융·시장 어디로도 갈 수 없었다.** 헤더를 2단으로 쌓고 링크 줄을 따로 깔았다(좁으면 가로 스크롤). 햄버거 대신 링크 줄을 택한 이유는 탭이 한 번 적고 열고 닫는 상태·포커스 트랩이 없어서다. 첫 줄 배치는 그대로 두고 브랜드만 줄어들게 묶어 3단이 되는 것을 막았다. ② 시장 페이지에서 설정 종목 카드가 일일 브리핑보다 앞이라 모바일에서 본문이 화면 몇 개 아래로 밀렸다 — 순서를 바꾸되 `band--surface`(강조 밴드)는 본문 쪽에 남겼다. **둘 다 데스크톱 확인만으로는 못 잡는 종류**라 `tests/test_mobile_layout.js` 가 잠근다(8개 페이지 헤더 nav 포함). **⚠️ `style.css` 에 `@media (max-width: 620px)` 블록이 두 개다** — `indexOf` 로 첫 개만 보면 엉뚱한 블록을 검사하고 조용히 통과한다(테스트 작성 중 실제로 걸렸다).
- **★★ 주간 원료를 TelegramPick 으로 연결하기로 확정(2026-08-05 grill) — ⚠️ 미착수:** 같은 워크스페이스 `!03. NextProject/TelegramPick` 에 신규 로컬 웹앱(FastAPI + React)이 생겼고 **실사용 중**이다(채널 41 · 포스트 1,170 · 아이템 653 · 08-05 export 완료). **2026-08-03 그릴에서 "3단계 출처 보존 파이프 **신설** + 7일 데이터 축적 대기"로 잡았던 게 이미 존재하고 돌아가고 있다** — 주간 게이트가 요구하는 `출처URL`(`posts.external_link`) · `원문제목`(`title_original`) · `원문일시`(`posted_at_utc`) · `제목ko`/`한줄ko` · 사람 승인(`items.state`) · 중복 클러스터링(`event_key`·`item_posts`) · 비공개 출처 플래그(`is_private_source`)가 전부 스키마에 있다. **왜 필요한가:** `fetch_weekly.py` 는 Google News RSS 에서 원료를 뽑는데 **08-04 실행에서 `ai-infra`·`commodity`·`flows` 세 도메인이 통째로 비었다**(`[ok] ai-infra: 후보 0건`). Pick 이 그 공백을 텔레그램 소스로 메운다. **확정안:** ① `주간-초안` 으로 push — 대상은 `included` 중 **외부링크 보유분**(실측 7/31) + `candidate` 상위 5건, `is_private_source` 제외. `status` 는 included→`approved` / candidate→`draft`. ② **⚠️ `telegram_link` 폴백 절대 금지** — Pick 의 개인용 export 는 `external_link or telegram_link` 로 폴백하는데, 그대로 보내면 형식 검사는 통과하지만 **t.me 채널 핸들이 공개 사이트와 구독자 메일에 실린다**(08-04 A3 핸들 마스킹을 정면으로 되돌리는 경로). ③ Pick 의 `TOPICS` 를 **BSL 도메인 11개 + 정세 + 일반 = 13개**로 교체해 `topic → 분야` 직접 복사가 되게 한다. 정세·일반은 Pick 전용으로 BSL 에 안 보낸다 — **BSL 에 국제정세 도메인을 신설하는 안은 `fetch_weekly`·`Code.gs`·`script.js`·경제 페이지(현재 단일 매크로 모드) 개조까지 4곳 이상이고 탭 key 불일치 시 조용히 사라지는 사고 유형이라 별도 작업으로 분리**했다. ④ **2단 승인은 기존 게이트 그대로** — 콘솔 예약 없으면 월 04:00 LLM 자동(`prepare_weekly_release.py`). 사용자가 요청한 "시간 내 승인 안 하면 LLM 자동 승인"이 이미 그 구조다. **규모:** Pick 49 + candidate 35 + Google News 39 = **후보 123/주** → 발행 40~48건. 게이트는 무료 게이트 통과분에만 LLM 을 돌리므로 8~13분(release→send 버퍼 5시간 내). **부수 확정:** `Telegram 자동화` 관리 웹은 **BSL Apps Script 콘솔로 합칠 수 없고**(crontab·SQLite, Apps Script 도달 불가 — 08-03 결론 유지) TelegramPick UI 로 흡수한다. 서버 DB 는 **오라클 DB 가 아니라 그 VM 위의 SQLite 파일**이다. 상세 설계·작업 순서는 `Telegram 자동화/HANDOFF.md`·`TODOS.md`(TODO-010~012) 와 `TelegramPick/docs/product-brief.md` "Phase 2" 절.
- **★★ 일일 메일 3슬롯 창 라이브(2026-08-04) — A1·A2·A3·B1 배포 완료:** 전날 grill 에서 확정한 배달 복구를 전부 구현·배포했다. **BSL 쪽 변경은 `mailer/Code.gs` 의 `marketBody_`·`dailyGroups_` + 신규 헬퍼 3개**(`dailyWindow_`·`slotOf_`·`topicPart_`). 창 = **당일 장전(전문) + 전일 마감(토픽만) + 전일 장중(토픽만)**, 최신 먼저. **동작 확인(09:15 `checkMarketBody()`):** `marketBody_()` **3,489자** = 1,707(장전) + ~1,200(마감 캡) + 775(장중). 어제는 0자였다. **⚠️ B1 은 내용 증가가 아니라 단일 실패점 제거였다** — 08-03 에 장중 775자가 시트에 멀쩡히 있었는데도 장전이 빠졌다는 이유로 메일에 아무것도 안 붙었다. **`topicPart_` 길이 캡이 실전에서 이미 한 번 일했다:** 08-03 마감 슬롯 본문이 **21,647자**였는데(단일 LLM 응답은 `max_tokens=4096` 이라 불가능한 길이 — 상한 없는 경로는 텔레그램쪽 빈응답 폴백의 채널 요약 concat 하나뿐이다) 폴백 본문에는 `분야별/채널별` 헤더가 없어 마커 검색이 실패하고 1,200자 캡으로 떨어졌다. 캡이 없었으면 본문이 24,000자가 되어 **Gmail 102KB 잘림**에 걸렸을 것이다. **`topicPart_` 는 `채널별`·`분야별` 을 둘 다 받는다** — 텔레그램 프롬프트가 핸들 노출을 막으려 문구를 바꿨는데 이 창은 어제 행까지 읽어 전환 기간엔 옛 문구가 섞인다(커밋 `d49cb62`). **신규 진단 `checkMarketBody()`**(커밋 `91d86dc`) — 상세 브리핑이 안 붙을 때 원인 3가지(행 없음 / 오늘 날짜 없음 / 시간대 불일치)를 30초에 가린다. 밑줄 없이 지어 실행 드롭다운에 뜬다. **검증:** 신규 `tests/test_daily_body_window.js` 15케이스 — 창을 옛 '장전 전용'으로 되돌리면 실패하는 것까지 확인. Python 45건·JS 11파일 통과. **⚠️ 운영 주의:** `mailer/Code.gs` 는 **CFG 전체 붙여넣기 금지**(SALT·TEST_MODE·MARKET_SHEET_ID 가 플레이스홀더). 이번에 그 함정을 실제로 밟아 `MARKET_SHEET_ID` 를 다시 채워야 했다 — **SALT 는 로컬 어디에도 없고 운영 Apps Script 에만 있다.** 잃으면 기존 발송분의 수신거부 링크가 전부 깨진다. 앞으로도 **함수 단위 부분 교체**만 할 것. 텔레그램쪽 변경(A1·A2·A3)과 설계 근거는 그 프로젝트 `HANDOFF.md`·`TODOS.md` 참조.
- **★★ 일일 메일 내용 결손 진단(2026-08-03 grill) — ✅ 2026-08-04 배포 완료(위 항목):** "일일 시황 메일이 옛 텔레그램 3회 메일보다 훨씬 얇다"는 문제를 코드로 실측했다. **결론: 콘텐츠가 아니라 배달이 끊긴 것이다.** 텔레그램 파이프는 07/13/19 세 번 다 정상 동작하며 매번 채널 28개 요약(4,000자)을 만든다. 그런데 ① 그 상세 본문이 메일에 안 붙고 ② 오후·저녁 슬롯은 아무도 읽지 않는다. **결손 3가지:** **A(버그, 가장 큼)** `marketBody_()`가 빈 문자열을 돌려줘 '상세 브리핑' 섹션이 통째로 없다 — 사용자 메일 실물로 확인. 원인 후보는 `market/Code.gs` 22~23행: 웹앱은 `getSheetByName`으로 탭을 찾을 뿐 **만들지 않아** `시장-본문` 탭이 없으면 매번 `{ok:false,"tab not found"}`를 돌려주는데, 텔레그램쪽 `summarizer.py:136`이 **반환값을 버려서** 로그에만 남고 조용히 지나간다(오늘 `fetch_weekly.py`에서 고친 것과 **정확히 같은 버그 유형 — 이 계열 세 번째**). **B(설계)** `marketBody_()`의 `시간대 === "장전"` 필터와 `dailyGroups_()`의 `날짜 === 오늘` 필터 탓에 07:40 발송 시점엔 **당일 07:00분만** 존재한다 → 하루 산출물의 2/3 미배달. **C(설계)** `MARKET_MAX_ITEMS=3`으로 28채널 요약이 최대 9건 카드로 압축(분류 필터는 현재 아무것도 안 버린다 — 28채널 전부 경제/금융/기술로 매핑됨). **확정안:** 1통 유지 + 3슬롯 누적 · 본문은 **장전만 전문, 전일 오후·저녁은 토픽 섹션만**(~6,000자, Gmail 102KB 잘림 회피) · **채널명은 태그로 치환**(`@growth_semi`→`[반도체 공급망]`, 프롬프트+정규식 **이중 방어**) · **C는 이번에 손대지 않는다**(A+B만으로 카드 9→27·본문 0→6,000자. 셋을 동시에 바꾸면 무엇이 효과였는지 구분 불가) · **관리 콘솔은 합치지 않는다**(`_update_crontab`이 서버 crontab을 직접 고치고 채널 CRUD는 SQLite — Apps Script가 오라클 서버에 접근 불가. 채널을 시트로 옮기면 **시트 장애 = 수집 중단**). **⚠️ A 복구의 필수 동반 작업:** 지금 상세 본문이 비어 노출이 없지만, 복구하는 순간 `prompt.py`가 채널 블록을 `@handle`로 만드는 탓에 **28개 핸들이 구독자 21명에게 나간다**(`guard.mask`는 BANNED 표현만 가린다). 소싱 자산 노출 금지 원칙과 충돌하므로 태그 치환이 A와 같이 가야 한다. **BSL 쪽 작업(B):** `mailer/Code.gs`의 `marketBody_`·`dailyGroups_` 창 변경 — **Apps Script 라이브 배포 필요**(푸시로는 반영 안 됨). **✅ 원인 확정(2026-08-03 17:04, 신규 `checkMarketBody()` 실행 — 커밋 `91d86dc`):** **첫 가설(탭 없음)은 틀렸다.** 탭·헤더·쓰기가 전부 정상이고 `시장-본문`에 **47행**이 쌓여 있다. 진짜 원인은 **오늘(08-03 월) '장전' 행이 없다**는 것 — `marketBody_()`가 `시간대 === "장전"`만 보므로 0자를 돌려준다. 07:00 실행에서 `full_summary`가 비어 텔레그램쪽 `write_market_body`가 **아무것도 안 쓰고 `True`를 반환**했고 그 경로는 **로그조차 남기지 않는다**. 같은 날 13:00 장중은 775자로 정상 기록 → 쓰기 경로는 살아 있다. 이를 막을 폴백은 같은 날 14:51 로컬 작성분이라 **서버 미배포**. 주말(08-01 토·08-02 일)에 장전만 있는 건 정상 — 오후·저녁엔 채널 메시지가 없어 조기 종료된다(평일 수·목·금은 3슬롯 모두 존재). **⚠️ 이 진단으로 B의 성격이 바뀌었다 — 우선순위 상승:** `marketBody_()`가 장전만 보는 탓에 **장전 하나가 빠지면 그날 상세가 통째로 사라진다.** 오늘 장중 775자가 시트에 멀쩡히 있는데도 메일엔 안 붙었다. **B1(3슬롯 창)은 내용 증가가 아니라 단일 실패점 제거**이며, 폴백이 또 뚫릴 때의 안전망이다(폴백은 LLM이 빈 문자열을 줄 때만 듣는다). 그래서 **A1·A2와 같은 배포에 B1을 함께 넣는다.** **별건 관찰:** 본문 길이가 평일 1,600~3,400자였는데 08-03 장중이 **775자**로 낮다 — 단일 관측이라 추세 단정은 보류, 내일 아침 장전 길이로 판정. 주간 초안에서도 `[ai] deepseek 응답 2자`가 찍힌 적이 있어 엔진 쪽 짧은 응답 문제가 별개로 있을 수 있다. 상세 설계·순서는 텔레그램 프로젝트 `HANDOFF.md`·`TODOS.md`(TODO-007~009).
- **작업 브랜치:** main (`origin/main` 푸시 완료)
- **라이브:** https://brevislab.com (커스텀 도메인·HTTPS 정상, 2026-07-21 연결) — 옛 sk-jeong-0513.github.io/briefing-signal-lab 는 배포 소스로 유지(www→apex 301)
- **★★ 주간 발행 시각 화 20:00 → 월 09:00 KST 이동(2026-08-03, 사용자 지시) — ⚠️ 운영 Apps Script 트리거 재설치 대기:** 발송 시각은 **네 곳이 함께 움직여야** 한다 — `weekly-send.yml` cron · `weekly-release.yml` cron · `Code.gs`의 `sendWeekly` 시간 트리거 · 알림 3개. 한 곳만 바꾸면 옛 요일에 메일이 나가거나 호가 통째로 빠진다. **새 사이클(KST):** 일 06:00 초안 생성 → 일 09:00 초안 확인 알림 → 일 20:00 마감 8시간 전 알림 → **일 24:00 승인 마감** → **월 04:00 조건부 자동 검수**(`0 19 * * 0`) → **월 09:00 공개·메일 발송**(`0 0 * * 1`) → 월 10:00 결과 알림. **버퍼를 2h55m → 5h 로 넓혔다** — 2026-07-28 실측 지연이 release 2h38m·send 1h51m 라 옛 버퍼로는 **17분**밖에 안 남았고, release 가 send 보다 늦으면 rev.1 원장이 없어 `sendWeekly`가 "발송 가능한 rev.1 원장/항목 없음 — 생략"으로 끝나 **그 호가 조용히 사라진다**. **정시성은 Apps Script 트리거가 담보하고 GitHub Actions 는 백스톱이다** — Actions cron 은 상시 1~2시간 지연되고 새 발송 시각 00:00 UTC 는 가장 혼잡한 슬롯이다. 먼저 도는 쪽이 발송하고 나중 쪽은 발송로그를 보고 no-op. **issue_key 는 그대로다** — 파이썬 `(1 - weekday) % 7`(다가오는 화요일의 ISO 주)과 Apps Script `weeklyIsoIssue_(0)`(오늘의 ISO 주)이 **월·화가 같은 ISO 주**라 일치한다(일요일만 ISO 주가 끝나는 날이라 알림에서 `addDays=1` 필요 — 기존 로직 유지). 알림 함수는 요일 이름 → 역할 이름으로 바꾸되(`weeklyAlertDraft`/`Deadline`/`Result`) `createWeeklyTriggers`의 **삭제 목록에 옛 이름 3개를 남겼다** — 빼면 화요일 트리거가 사라진 함수를 계속 호출해 매주 오류 메일이 온다. **검증:** 신규 `tests/test_weekly_schedule.py` 9건(순서·버퍼·issue_key 일치·양쪽 발송시각 동기화·알림 전후·옛 이름 정리) — **한쪽만 화요일로 되돌리면 실패하는 것까지 확인**했다. Python 45건·JS 10파일 통과. 커밋 `2d88314`. **⚠️ 잔여(사용자, 30초):** 운영 Apps Script에서 **`createWeeklyTriggers()` 1회 재실행**. 안 하면 트리거가 화 20:00에 머물러 정시 발송이 안 되고 옛 알림 2개가 남는다. 저장소 푸시로는 반영되지 않는다. **2026-W32 는 이 변경과 무관하게 이미 나갔다** — `주간-발행항목` 기준 40건이 `2026-08-03T14:12` 에 `published` 로 기록돼 있다(운영자가 콘솔/Apps Script 에서 `sendWeekly()` 실행). 그 뒤 확인차 `weekly-release`(no-op: "이미 준비/처리됨")와 `weekly-send`(`{"ok":true}`)를 돌렸고, **수신자별 발송로그가 중복 발송을 막으므로 재발송은 일어나지 않는다**(`weeklyLatestBundle_`는 `emailed` 상태면 null 반환). 실제 발송 결과는 Apps Script 실행 로그와 `주간-발송로그` 탭이 최종 기록이다. 새 스케줄이 실제로 도는 첫 호는 **2026-W33, 2026-08-10(월) 09:00**.
- **★★ 초안 시트 쓰기 실패가 '초록불'로 끝나던 문제(2026-08-03) — ⚠️ 정리 필요(주간-초안 중복 39행):** 2026-08-02 `weekly-draft` run(`30719662673`)이 GitHub 에서 **success 인데** 로그에는 `[write] 실패: The read operation timed out` 이 찍혀 있었다. `post_rows`가 예외를 `print`만 하고 삼켰고 `main`도 정상 종료해 exit 0 이 됐다. **⚠️ 중요 — 이 타임아웃은 '유실'이 아니었다.** Apps Script 가 POST 를 받아 처리하고 응답만 30초를 넘긴 것이고, **시트에는 40행이 정상 기록돼 있었다**(W32 가 그 40건으로 발행됨). 클라이언트 타임아웃을 쓰기 실패로 읽으면 안 된다. 그럼에도 **초록불 자체는 버그다** — 진짜 실패해도 아무도 모른다. **수정:** 타임아웃 30s → 120s · 3회 재시도(백오프 10s/20s) · 최종 실패 시 `False` 반환 → `main`이 1로 종료해 **run 이 빨갛게 뜬다** · 실패 로그에 "타임아웃이라도 기록됐을 수 있으니 시트 확인" 안내. `tests/test_fetch_weekly.py`에 회귀 5건. 커밋 `5421c1c`. **⚠️ 잔여(사용자):** 유실로 오판하고 재수집을 돌려(run `30787200389`, `added:39`) **`주간-초안`에 W32 draft 39행이 더 붙었다.** 발행된 40건과는 다른 기사라 사이트·메일에는 영향 없지만, `used_keys()`가 status 와 무관하게 `WEEKLY_DRAFT_CSV` 전 행을 읽으므로 **다음 주(W33) 수집에서 이 39건이 '과거 사용'으로 제외된다.** `주간-초안`에서 발행주 `2026-W32` · status `draft` 인 **마지막 39행(2026-08-03 14:29 추가분)을 삭제**하면 원복된다. 영향은 작다 — W33 수집 시점(08-09)엔 대부분 발행 게이트의 freshness 10일에 걸린다.
- **★★ 유료 CTA 임시 비활성(2026-08-03) — A2:** `LINKS.paidForm`이 **`freeForm`과 폼 ID가 완전히 동일**했다. 즉 유료 문의를 누른 사람이 옛 **'Biz Signal Lab'(AI 사업 진단, 필수 15문항 — 주당 투입 시간·초기 예산·가장 큰 고민 장문)** 폼을 만나고 있었다. 2026-07-28에 무료만 인라인 폼으로 옮기고 유료는 그대로 남은 잔여분이며, 통과 기준("AI 사업 진단으로 회귀하지 않음") 위반이기도 했다. **조치(사용자 선택 = 버튼 감춤):** 유료 대기자 폼이 준비될 때까지 **잘못된 곳으로 보내느니 감춘다.** `site.js`의 `paidForm`을 **빈 문자열**로 두고, `applyLinks()`가 링크가 빈 `[data-link]` 요소를 `hidden` 처리 + `href` 제거(탭 이동에서도 빠짐), `renderCompare()`는 요금제 비교 카드의 CTA를 **아예 그리지 않는다**(`|| "#"`로 죽은 버튼을 남기지 않음). 덮이는 진입점 5곳 = 랜딩 CTA·기술/금융/경제 유료 섹션·무료/유료 비교 카드. **되돌리기는 `paidForm`에 URL 한 줄** — 코드 수정 없이 버튼이 그대로 살아난다. **함정 하나(7/28 `.btn[hidden]`과 같은 유형):** `.cta__actions`가 `display:flex`라 **작성자 스타일이 브라우저 기본 `[hidden]`을 이겨** `box.hidden = true`가 안 먹는다 → `.cta__actions[hidden] { display:none }` 규칙을 명시했다. 빈 액션 줄은 컨테이너까지 감춰 여백만 남지 않게 하고, `initSubscribe()`가 중복 무료 버튼을 감춘 **뒤에** 판정하도록 `init()` 순서를 잡았다. 카피는 손대지 않았다 — `paidNote`가 이미 "유료 발송은 순차 오픈 예정"이라 버튼이 없는 편이 오히려 정합하다. **검증:** 신규 `tests/test_paid_cta.js` — **무료·유료가 같은 폼을 가리키면 실패**(유료 폼을 새로 만든 뒤에도 유효한 회귀 방어) · 빈 링크 감춤 규칙 · CSS 두 규칙 · 순서 · 진입점 4개 HTML 커버리지 · **HTML에 폼 URL 하드코딩 금지**(감춤 우회 방지) · 무료 구독 경로 생존. JS 10개 파일·Python 31건 통과. **⚠️ 잔여:** 유료 수요 신호를 지금은 전혀 못 받는다. 유료 대기자 폼(이메일 + 관심 분야 티어 매핑 + 희망 가격대)을 만들면 D 목록의 "유료 폼 문항 추가"와 함께 해소된다.
- **★★ 재구독 복구 구현(2026-08-03) — ⚠️ 운영 Apps Script 배포 대기:** 2026-07-28에 "정책 결정 필요"로 남겨둔 재구독 불가 문제를 **자동 복구**로 확정해 구현했다(선택지 (a)). 구독 폼에 `메일 수신 동의` 필수 체크가 있어 **재제출 = 새로운 명시적 동의**로 본다. **설계:** 차단 로직(`unsubSet_()`·주간 루프 280행)은 **건드리지 않고**, 발송 직전에 `syncResubscribes_()`가 pref 시트를 실제 상태로 맞춘다 — 시트에 `수신거부`로 보이는데 메일은 나가는 불일치를 없애고 변경 범위도 최소가 된다. **판정:** 그 이메일의 **가장 최근** 응답 시각 > 수신거부 시각이면 복구. 발송 루프가 `seen[email]`로 첫(=가장 오래된) 행만 취하므로 `respLatestTs_()`가 최댓값을 따로 구한다. **안전 쪽 규칙 — 판별 불가는 전부 수신거부 유지:** 타임스탬프 열 없음·파싱 실패·**동률**. 특히 **시각 도입 전 행은 `갱신`에 날짜만 있어** `unsubTime_()`이 그날 끝(23:59:59)으로 해석한다 — 그러지 않으면 같은 날 **먼저** 낸 응답이 해지보다 나중으로 오판돼 "수신거부했는데 또 온다"가 된다. 앞으로는 `prefUpsert_()`가 `yyyy-MM-dd HH:mm:ss`로 저장해 같은 날 해지→재신청도 구분된다. 분야를 모두 끈 뒤 해지한 경우는 빈 분야로 되살리면 메일이 안 가므로 가동 분야 전체로 복원한다. **검증:** 신규 `tests/test_resubscribe.js`(시각 해석·판정 경계·최신 응답 선택·시트 쓰기·배선 순서) + 전체 스위트 회귀 — **JS 9개 파일·Python 31건 전부 통과.** **⚠️ 배포 필요:** 저장소 푸시로는 운영 Apps Script에 반영되지 않는다. `prefMap_`(155~165행)·`prefUpsert_`(166~179행) 교체 + 재구독 복구 블록(480~554행) 추가 + `sendDailyMarket`·`sendWeeklyUnlocked_`에 `syncResubscribes_();` 1줄씩. **CFG 전체 붙여넣기 금지.** **웹앱 재배포도 필요** — `doGet`이 `prefUpsert_`를, `doPost`가 `sendWeekly`를 쓰는데 웹앱은 시간 트리거와 달리 배포된 버전으로 실행된다. 절차는 `mailer/README.md` "재구독 복구". **라이브 반영·검증 완료(2026-08-03 11:11):** 운영 Apps Script에 4곳 반영 후 실데이터로 확인 — 응답 타임스탬프 22명 정상 판독, 배선 점검 4/4 `true`(일일은 `unsubSet_` 앞, 주간은 `prefMap_` 앞). **마침 레거시 경로가 실검증됐다** — `paun.jeong@gmail.com`의 `갱신`이 `2026-07-06` **날짜만**(시트가 자정 Date로 보관)인 옛 행이었고, `unsubTime_`이 7/6 23:59:59로 해석해 7/28 07:56 응답과 비교 → `복구대상=true` → `[재구독] 수신거부 해제 1건`으로 상태가 `구독`으로 복원됐다. **⚠️ 잔여: 웹앱 재배포** — `doGet`(수신거부·분야 토글)과 `doPost`(주간 발송)는 시간 트리거와 달리 **배포된 버전**으로 돌아, 재배포 전까지 그 두 경로는 옛 `prefUpsert_`를 써서 `갱신`에 시각이 안 찍힌다(앞으로의 해지가 다시 날짜-only가 됨). **운영 메모 — Apps Script는 `_`로 끝나는 함수를 private으로 취급해 실행 드롭다운에 표시하지 않는다.** 임시 진단 함수는 밑줄 없이 만들 것(이 때문에 처음 안내한 `checkResubscribe_`·`testQuotesToday_`가 실행되지 않았다). 읽기 전용 진단 `checkResubscribe()`는 "구독했는데 메일이 안 와요" 문의 때 30초 진단용이라 운영본에 남겨둘 만하다.
- **★★ 일일 크론이 KST 월요일을 건너뛰던 버그 수정(2026-08-03):** cron의 요일 필드는 **UTC 기준**인데 일일 워크플로 3개가 21~22 UTC에 도는 탓에 KST로는 하루 밀려, `1-5`(UTC 월~금)가 실제로는 **KST 화~토**를 덮고 있었다. 즉 **KST 월요일 아침은 실행이 없고 토요일 아침에 불필요하게 돌았다.** 3주치 실행 이력 전부 이 패턴(예: UTC 07-31 금 23:09 → KST 08-01 토 08:09, UTC 일요일 실행 0건). **실제 피해:** 2026-08-03(월) 라이브 `quotes.json`이 `briefing_date=2026-08-01`로 이틀 묵어 07-31 fail-safe가 작동, **그날 일일 메일에서 '주요 시장 지표' 블록이 통째로 생략**됐다(잘못된 값이 나가진 않음). **조치:** `daily-quotes.yml`·`dashboard-data.yml`·`market-data.yml`의 요일을 `1-5`→**`0-4`**(UTC 일~목 = KST 월~금)로 변경, 주석에 UTC 요일임을 명시. **시각(UTC 21:10/21:30/22:00)은 변경하지 않았다** — 미 동부 마감이 서머타임 20:00 UTC·겨울 21:00 UTC라 앞당기면 겨울에 마감 전 데이터를 수집한다(`daily-quotes.yml` 주석의 "겨울 장 마감 10분 후"가 이미 그 경계). 주간 3개(`weekly-draft` `* * 6`, `weekly-release`/`weekly-send` `* * 2`)는 원래 시프트를 반영해 정확했고 미변경. **검증:** 9개 워크플로 YAML 파싱 통과, 향후 15일 발화일 시뮬레이션에서 KST 월~금만 발화(08-10 월 포함·08-08 토 제외) 확인. **잔여 관찰:** GitHub 예약 크론이 3주 내내 **약 1시간 지연**돼 실행된다(예약 22:00 → 실측 23:03~23:12). `daily-quotes`는 07:12 KST 도착으로 07:40 메일까지 여유가 ~28분뿐이므로, 지연이 더 커지면 같은 증상(블록 생략)이 평일에도 난다. 겨울(EST) 전환 때 시각 재검토 필요.
- **★★ 일일 시황 지표 당일 아침 기준 보정(2026-07-31):** 07:40 KST 메일보다 데이터 커밋이 늦어 전일 스냅샷이 오는 경합을 해결했다. `.github/workflows/daily-quotes.yml`을 평일 06:10 KST에 추가하고 `fetch_dashboard.py --quotes-only`로 `quotes.json`만 조기 갱신한다. JSON에 한국 기준 `briefing_date`를 추가하고 미국 거래일 `asof`와 분리했다. 메일러 `quotes_()`는 캐시 버스터를 사용하며 `briefing_date`가 한국 기준 당일이 아니면 전일 데이터 대신 지표 블록만 생략한다. 표시는 `MM-DD 아침 기준 · 미 증시 MM-DD 마감`. Python 31건·JS 8개 테스트 파일·YAML 파싱·`git diff --check`·Yahoo 21/21 실수집을 통과했고 커밋 `95c379c`를 `main`에 푸시했다. **운영 경계:** GitHub 저장소와 Pages 쪽 반영은 완료됐지만 운영 Apps Script는 자동 갱신되지 않는다. 운영 `Code.gs`의 실제 `CFG`를 보존하고 저장소 580~648행(지표 마커부터 `quotesPlain_()` 끝까지)만 교체·저장해야 실제 메일에 적용된다. 650행 `dailyHtml_`은 포함하지 않는다. 웹 앱 재배포와 트리거 재생성은 불필요하다. 상세 절차는 `mailer/README.md`.
- **★★★★★ 주간 발행 자동화 운영 전환 완료(2026-07-27):** 행 승인과 호 발행 분리, `BSL_market/주간-발행` 단일 원장, 일 06:00 생성→화 17:00 마감→17:05 조건부 자동 승인→20:00 고정 공개·메일. 자동 게이트 전부+독립 검수 85점 이상, 통과 1건 이상 발행·0건 skip. 늦은 승인분은 웹 `rev.2+` 업데이트, 이메일 재발송 없음. 수신자별 성공 로그·동시 실행 잠금으로 중복 방지. 로컬 Python/JS 문법, Python 12건과 JS 안전성 스위트 통과. **라이브 완료:** 초안 메타데이터 4열 추가, 발행 탭 3개 생성, 발행·발행항목 CSV 게시, Actions Secret 5개 등록, admin/mailer Apps Script 재배포, 주간 트리거 4개 생성. `2026-W31`은 `manual_ready · rev.1` 48건으로 확정 후 실제 발송 21명 성공·0명 실패, 최종 `emailed`. GitHub Actions→메일러 웹 앱 연결 테스트(run `30247525471`)도 성공했고 `emailed` 원장으로 중복 발송이 차단됐다.
- **★★ 주간 초안 선별 로직 보완(2026-07-27, 2차 세션):** 같은 기사(포토니솔 광 아이솔레이터)가 W29·W30·W31에 반복 게재된 원인 = **Google News RSS가 관련도순**이라 수개월~2년 전 기사가 매주 상위에 남고(`&scoring=n`도 무시됨), 수집 24건 중 최근 7일은 4건뿐이었다. bio는 `DOMAINS`에서 유일하게 `hint`가 없어 기술 기본 프롬프트로 나가 LLM이 `[]` 반환, flows는 §6 BANNED의 `매수세/매도세`가 그 분야 필수 어휘라 카드가 통째로 drop, macro 등은 오래된 원문일시가 발행 게이트 `freshness`(10일)에 걸려 전멸 — 이것이 W31에서 bio·macro·flows가 빈 이유다. **조치:** ① `feed_gnews`에 `when:7d` + pubDate 하드 필터(`WEEKLY_COLLECT_DAYS`, 기본 7 — 일 수집→화 발행 시차 2일이라 게이트 10일보다 짧아야 함), 일시 불명 기사도 제외 ② `used_keys()`가 `WEEKLY_DRAFT_CSV`+`WEEKLY_RELEASE_ITEMS_CSV`(기존 시크릿 재사용, 신규 없음)를 읽어 **이미 초안/발행된 기사는 재수집 안 함**(출처URL·정규화 원문제목 대조, 조회 실패는 fail-open) ③ bio hint 추가 ④ 프롬프트에 금지 어휘 명시(`매수세·매도세` 대신 `순매수·순유입`) — **guard.py는 미변경**(§6 방어선 유지) ⑤ `prepare_weekly_release.py`에 `duplicate_prior_issue` 게이트(지난 호 재게재 차단) + 원장 message에 분야별·탈락사유별 집계 ⑥ 초안 0건 분야를 로그에 명시. **검증:** 21개 테스트 통과(신규 9), guard selftest OK, JS 안전성 스위트 OK, 라이브 11개 도메인 재수집 = 129건 전부 7일 이내·포토니솔 소멸·flows 9건 나머지 12건씩. **잔여 관찰:** 초안 시트를 통째로 비우면 dedup 기억이 draft 쪽에서 사라진다(발행항목 원장은 남으므로 발행분 중복은 계속 차단됨) — 옛 draft는 최근 몇 주만 남기는 편이 낫다.
- **★★ 일일 시황 발송 시각 설정화(2026-07-27, 2차 세션):** 일일 메일이 프리마켓 전이 아니라 장 오픈 후 도착하던 원인 = `createDailyTrigger()`가 `atHour(8)`만 쓰고 **`nearMinute()`가 없어 Apps Script가 08:00~09:00 사이 임의 시각에 실행**(주간 트리거 4개는 전부 `nearMinute(0)`이 붙어 있는데 일일만 누락) → 08:50 도착 시 KRX 장전 단일가(08:30) 이후. **조치:** 발송 시각을 코드 하드코딩에서 **관리자 콘솔 설정**으로 이전(서머타임 등으로 적정 시각이 바뀌므로). `settings` 탭 `daily_send_time`(`HH:MM` KST) — 기존 `pipeline_enabled`·`llm_primary`와 같은 패턴, 새 인프라 없음. `mailer/Code.gs`에 `marketSetting_`(BSL_market openById 조회, 실패는 fail-open) · `dailySendTime_`(형식 검증, 폴백 `CFG.DAILY_SEND_TIME="07:40"`) · `applyDailySchedule`(멱등 재생성, **nearMinute 적용**) · `syncDailySchedule`(새벽 03:00, 설정 변경·트리거 소실 시 재생성) 추가. `createDailyTrigger()`는 이름·역할 유지(설치 시 동기화 트리거도 함께 생성). 콘솔 ⑤ 탭을 "엔진·발송시각"으로 확장(`getDailySendTime`/`setDailySendTime`, time input, 미지정 시 "(기본값)" 표시). **검증:** `tests/test_daily_schedule.js` 신규 — 설정 해석 6케이스(정상·미설정·범위밖·형식오류·0패딩·조회실패) + 트리거 멱등·nearMinute·타임존 + 동기화 3케이스(no-op·시각변경·트리거소실) + 콘솔 검증 8건, 전부 통과. **라이브 완료(2026-07-27):** 메일러는 CFG 2줄 + 트리거 블록만 수술 교체(레포 사본 CFG는 SALT·TEST_MODE·MARKET_SHEET_ID가 플레이스홀더라 **전체 붙여넣기 금지** — 재발 방지 메모), 콘솔은 전체 교체 후 재배포(스크립트 속성 방식이라 전체 교체 안전). 사용자가 트리거 2개(`sendDailyMarket` 1개 + `syncDailySchedule` 1개, 옛 08시 트리거 없음)·콘솔 ⑤ 패널·발송 시각 지정까지 확인 완료. **메일러는 트리거 함수만 바뀌어 웹앱 재배포 불필요**(시간 트리거는 배포 버전이 아니라 최신 저장 코드로 실행). **⚠️ Apps Script 시간 트리거는 ±15분 오차가 남는다** — 정확한 시각 지정은 불가하므로 목표 도착 시각보다 여유를 둘 것. 텔레그램 `장전` 생성은 07:00 KST(별도 레포, 자체 웹 UI로 조정 가능)라 그보다 뒤여야 그날 상세 본문이 담긴다.
- **★★ 일일 메일 '주요 시장 지표' 블록 추가(2026-07-27, 구독자 요구):** 헤더와 `경제 시황` 사이에 21개 지표를 **2열 11행**으로. 구성=지수 4(나스닥·S&P500·다우·러셀2000) + 통화 2(달러인덱스·원/달러) + 원자재 4(WTI·금·은·구리) + 금리 3(US2Y·5Y·10Y) + 섹터ETF 7(SOXX 반도체·ITA 방산·UFO 우주·XLK IT·XLY 소비재·XLF 금융·XLC 엔터) + EWY(한국·야간). **grill 11문항으로 확정한 설계 결정:** ① 요청의 `US30`·`US500`은 Dow·S&P 별칭이라 제거하고 나스닥 추가 ② 표시는 `종가 + 변동률` 2개만(절대변화는 지수마다 스케일이 달라 비교 불가, 모바일 셀 ~156px 초과) ③ **금리는 bp**(`+3bp`) — `%`로 쓰면 %p 혼동 ④ 초록 상승/빨강 하락(DESIGN.md success·danger; 파랑은 브랜드 링크색이라 사용 불가) + **부호 항상 표기**가 실제 방어선 ⑤ 12px·회색 라벨의 얇은 스캔 레인 + 구분선 — 위치는 요청대로 최상단이되 시각 위계로 시황 카드가 눈에 남게 ⑥ 섹터 ETF에만 티커 표기(`^DJI`는 야후 내부 심볼이라 노이즈) ⑦ KOSPI 야간선물은 무료 API에 없어 `EWY`로 대체(달러 표시라 환율 섞임·MSCI Korea라 삼성전자 편중 — 알고 쓸 것). **구현:** `fetch_dashboard.py`에 `QUOTES` 목록 + `quotes_snapshot()` → `public/assets/data/quotes.json`(**새 워크플로 0개** — 기존 dashboard-data 크론 평일 21:30 UTC=06:30 KST에 태움, 발송 07:40보다 앞섬). 표시 문자열까지 Python이 만든다(메일러는 수동 붙여넣기 배포라 티커·포맷 변경이 Apps Script에 닿지 않게). 메일러는 `quotes_()`(UrlFetchApp 1회) + `quotesHtml_`/`quotesPlain_`만. **함정 하나 — 선물·환율은 24시간 거래라 미국 주식 개장 전에도 '오늘' 봉이 잡힌다.** 그대로 쓰면 WTI가 진행 중 봉으로 −6.9%가 되고 asof도 어긋난다 → **기준 거래일=마지막 봉 날짜의 최빈값**으로 정하고 전 심볼을 그 날짜 이하 마지막 봉에 맞춘다. **fail-safe:** 개별 심볼 실패는 그 행만 생략 / 전부 실패면 직전 스냅샷 보존 / HTTP·JSON·네트워크 실패는 블록만 생략하고 메일은 정상 발송 / `asof`가 5일 초과면 블록 생략(연휴 3~4일은 통과, 월요일의 금요일 마감은 정상). 평문 메일에도 반영. **검증:** Python 29건(신규 8)·JS 3스위트 통과, 라이브 Yahoo로 21/21 수집, HTML 4.5KB(Gmail 클리핑 102KB와 무관). **⚠️ 배포 필요:** `mailer/Code.gs` 재붙여넣기(CFG 2줄 + 색 토큰 + 지표 함수 4개 + `sendDailyMarket`/`dailyHtml_`/`dailyPlain_` 시그니처). 웹앱 재배포는 불필요(트리거 실행 코드만 변경).
- **★ 메일 이모지 깨짐 해결(2026-07-27) — 원인은 `GmailApp.sendEmail`:** 상세 브리핑 첫 줄이 `������ 토픽별 통합 브리핑`으로 나오던 문제. **소거법으로 확정:** ① 시트 셀(`시장-본문`) 정상 — 이모지 그대로 ② Apps Script 읽기 정상 — `marketBody_()` 첫 줄 코드포인트가 `U+23 U+23 U+23 U+20 U+D83D U+DCCC`(정상 서로게이트 쌍) ③ 수신 메일만 깨짐. 즉 **`GmailApp.sendEmail`이 비-BMP(astral) 문자를 깨뜨린다.** 한글은 BMP라 무사하고 이모지만 깨지는 증상과 일치. 소스(`summarizer/prompt.py`)의 📌 U+1F4CC·📰 U+1F4F0도 올바른 UTF-8이었고, 텔레그램 POST(`json.dumps` ensure_ascii=True → 서로게이트 이스케이프)·`market/Code.gs` `JSON.parse`도 무결. **조치:** `mailSafe_()`(`/[\uD800-\uDFFF]/g` 제거)를 **발송 직전** 평문·HTML 양쪽에 적용(일일·주간 둘 다). 각 생성 지점이 아니라 경계 한 곳에서 막은 이유 = LLM 본문·제목 등 이모지 유입 경로가 여럿이라 새는 곳이 생김. **텔레그램 프롬프트는 미변경** — 텔레그램 메시지에서는 이모지가 정상 표시되므로 뺄 이유가 없다. 검증: `tests/test_mail_safe.js` 신규(실제 깨진 2문자열 + **BMP 보존 7종**으로 과잉 제거 방지: `弗`·`−`·`–`·`“”`·`⇅▲▼→`·`·` + HTML 구조 보존 + 짝 없는 서로게이트 + 배선). 라이브 수신 확인 완료. **⚠️ 재발 방지 원칙: Apps Script 메일에 astral 문자를 넣지 말 것.**
- **★★★ 구독자 증대 착수 — 전환 경로 복구(2026-07-28):** "기능은 많은데 구독자가 안 는다"에서 출발해 원인을 코드로 확인했다. **핵심 발견: 구독 버튼이 다른 제품 폼으로 나가고 있었다.** `LINKS.freeForm`이 가리키던 구글 폼의 제목은 **"Biz Signal Lab"**(AI 사업 아이템 진단·컨설팅)이고 **필드 17개 중 15개가 필수** — 문의 유형, 현재 상황, **주당 투입 가능 시간**, **초기 예산 범위**, **가장 큰 고민**(장문) 등. 브리핑 구독자가 완주할 구조가 아니었고, 프로젝트 통과 기준("'AI 사업 진단'으로 회귀하지 않음") 위반이기도 했다. **함께 확인된 구조 문제:** `robots.txt`·`sitemap.xml`·RSS 전부 없음 / **HTML 내 정적 텍스트 26자**(전부 JS 렌더링이라 검색엔진이 볼 게 없음) / `방문로그`의 referrer를 수집만 하고 콘솔에 표시 안 함 / 텔레그램 `writer.py`가 `"me"`(저장 메시지)로만 보내 **공개 채널 없음**. **이번에 한 것:** ① 콘솔 ④에 **유입 경로(referrer) 패널 + 외부 유입 수치** 추가 — `_refLabel_`이 전체 URL을 호스트로 묶고 자기 도메인은 '사이트 내 이동'으로 분리(어느 채널이 실제로 작동했는지 판단용). ② 랜딩 CTA에 **인라인 구독 폼**(이메일 + 수신동의 체크박스) — 새 탭 이동 제거. `SUBSCRIBE_FORM.formId`가 비면 폼을 숨기고 기존 버튼으로 **폴백**(테스트로 순서 강제). Forms는 CORS 헤더가 없어 `no-cors` POST → 응답을 못 읽으니 성공은 낙관 표시하고 실패 대비 폼 링크 유지. ③ **구독 전용 폼 신규 연결**(`Briefing Signal Lab 구독`, 질문 3개) — 실제 POST로 엔드포인트 검증(HTTP 200 + 기록 확인 페이지). ④ 메일러 원천 전환: `RESP_SHEET`=`설문지 응답 시트2`, email 열=**`이메일 주소(E-Mail Address)`**(폼 질문 제목 그대로 열이 생기며 질문을 고쳐도 기존 열 제목은 안 따라온다 — 시트 실제 헤더가 기준). 동의 보기 `동의합니다(Email Subscription)`는 `consented_()`의 '동의' 포함 판정을 통과함을 실행으로 확인. **기존 21명은 새 탭으로 이전 완료, 정상 발송 확인.** 검증: 신규 `test_subscribe_form.js`(폴백 순서·이메일 정규식·동의 필수·i18n·DESIGN 토큰만 사용=새 색상 0개)·`test_visit_referrer.js`, 전체 스위트 통과. **⚠️ 잔여:** ① 새 탭의 `bsl-connection-test@example.com` 행 삭제(연결 확인용) ② **유료 문의 버튼(`LINKS.paidForm`)은 아직 옛 "Biz Signal Lab" 폼** — 유료 대기자용 폼 필요 ③ 검색 유입 기반(robots·sitemap·RSS·정적 렌더링) 미착수. **다음 우선순위 = 유통(텔레그램 공개 채널).** 구독 그릇은 고쳤으나 유입이 적어 곱할 모수가 없다. `writer.py`의 `"me"`를 채널 ID로 바꾸면 매일 생성되는 일일 시황이 추가 운영 부담 0으로 공개 발행된다. 공개 범위는 **요약+링크** 권장(전문 공개 시 사이트 방문·구독 전환 유인이 사라짐).
- **★ 구독 폼 마무리(2026-07-28, 위 항목 후속):** ① **무료 구독 버튼 전부 인라인 폼으로 재배선** — nav '무료 구독'(9개 페이지)과 히어로 '무료로 구독하기'가 여전히 옛 외부 폼을 새 탭으로 열어 하단 인라인 폼과 혼선을 줬다. `wireSubscribeLinks()`가 `target`/`rel`을 떼고 `#subscribe`로 보내며, **폼은 랜딩에만 있으므로 다른 8개 페이지는 `index.html#subscribe`**로 넘긴다(넘어오면 `location.hash` 감지해 포커스). CTA 섹션의 중복 버튼은 숨김. ② **`.btn[hidden]` 규칙 추가 — 실제 버그였다.** `.btn { display:inline-flex }`가 브라우저 기본 `[hidden]`을 이겨서(작성자 스타일 우선) `el.hidden = true`를 걸어도 중복 버튼이 안 숨겨지고 있었다. 규칙을 지우면 테스트가 실패하는 것까지 확인. 같은 이유로 `.sub__again[hidden]`도 명시. ③ **같은 브라우저 반복 신청 방지** — 성공 시 `localStorage['bsl-subscribed']` 기록, 재방문하면 완료 화면. '다른 주소로 구독하기'로 되돌릴 수 있음(가족·회사 주소). localStorage는 프라이버시 모드에서 던지므로 try로 감쌈. **기기·브라우저를 넘는 차단은 서버 없이 불가능** — 실제로 발생하는 경우만 막았다. **중복 행 자체는 무해함을 코드로 확인:** 일일([Code.gs](../mailer/Code.gs) `if (seen[email]) continue`)·주간(`seen[email] ||`) 양쪽이 이메일 기준 dedup이라 메일은 한 번만 간다. 시트 dedup 트리거는 과한 작업으로 판단해 미적용.
- **랜딩 샘플 섹션(2026-07-28) — 칩만 반영, 카드는 보류:** ① **카테고리 바로가기 칩 `대시보드` → `시장`**(`market.html`, `nav.market`) 반영 완료. 헤더 nav의 대시보드 진입점은 유지. ② **3카드를 주간 발행 헤드라이너로 교체했다가 되돌림(`4c06dd4` → `974a064` revert).** 라이브 데이터는 충분했으나(published 48건 중 **헤드라이너 27건**, 전 카테고리 보유) **발행항목의 `한줄ko`가 한 줄뿐이라** 카드가 정적 샘플(요약 3줄)보다 짧아 레이아웃이 어색했다. `script.js`는 되돌리기 전과 바이트 동일, `index.html`은 칩 1줄만 차이. **되돌리며 함께 빠진 것(대책 시 다시 필요):** 파서의 `원문제목`·`출처URL` 보존(**없으면 라이브 카드에 정적 샘플의 옛 출처가 붙는다**) · 라이브용 '무료 공개' 배지 · 밸류체인 태그(최대 3개) · 빈 블록 생략 가드. **대책 후보:** (a) 발행항목에 요약 다중 라인을 담는다 (b) 카드 레이아웃을 한 줄 요약에 맞게 조정 (c) 헤드라이너 대신 그 도메인 신호 2~3건을 함께 노출. **⚠️ 대책 후 카피도 함께:** 라이브 콘텐츠를 넣으면 `samples.title`("오늘의 샘플 브리핑")이 사실과 어긋난다 — 샘플이 아니고 주간 콘텐츠다.
- **~~⚠️ 미해결~~ → 해결(2026-08-03, 아래 재구독 복구 항목) — 재구독이 안 된다(2026-07-28 발견):** 수신거부한 사람이 폼으로 다시 신청해도 메일이 안 간다. `관심분야(기술/금융/경제)` 시트의 상태가 `수신거부`로 남아 `unsubSet_()`에 걸리고, 일일 발송이 `unsub[email]`에서 차단한다. **구독자가 적어 아직 안 드러나지만 유입이 늘면 "구독했는데 안 와요" 문의로 나타난다.** 정책 결정이 필요해 손대지 않았다 — (a) 폼 제출 시각이 수신거부 `갱신` 시각보다 나중이면 자동 복구 (b) 운영자가 콘솔/시트에서 수동 해제. (a)가 사용자 기대에 맞지만 "수신거부했는데 또 온다"는 반대 위험이 있어 확인 필요.
- **★★ 웹 일일 시황 풍부화(2026-07-30, grill 7문항) — BSL 배포 완료 / 텔레그램 배포 대기:** 웹 일일 시황이 카테고리당 하루 1~2건뿐인데 메일은 더 풍부하다는 지적에서 출발. **원인은 콘텐츠 부재가 아니었다** — 메일에는 `시장-본문`(상세 브리핑: 토픽별 5섹션 + 채널별 요약)이 실리는데 **사이트는 `시장-일일`·`시장-종목` CSV만 읽어 상세 브리핑이 웹에 아예 없다.** 파이프가 카테고리당 `제목 15자 + 한줄 90자` 하나만 쓰고, 시장 무관 시간대는 건너뛰므로 1~2건이 정상 동작이었다. **확정된 7결정:** ① 상세 브리핑 중 **토픽별만 공개, 채널별 요약 비공개** — 거기엔 텔레그램 채널 핸들(`@…`)이 노출되고 그 목록이 소싱 자산이라 공개 시 서비스가 대체 가능해진다 ② **`시장-본문` CSV 게시 방식은 채택하지 않음** — 화면에서 채널 섹션을 숨겨도 **원시 CSV에 그대로 남아** 유출된다. 대신 **토픽을 `시장-일일` 행으로 확장**(새 CSV·게시·시크릿 0, 유출 경로 구조적 차단) ③ 카테고리 요약 1건은 **유지 대신 최대 3건으로 대체**(같은 원문에서 나와 1번 토픽과 중복) ④ **최대 3건, 있는 만큼** — 개수를 채우게 하면 조용한 날 잡음이 들어오고 '선별' 약속과 어긋난다(주간 파이프 반복 게재와 같은 실패 유형) ⑤ 시간대는 **제목 접두사 `[장전]`에서 순위 추출**(새 컬럼 없음) ⑥ 항목 분량 **2~3문장(180자)** — 90자면 랜딩 카드에서 겪은 '짧아서 어색함'이 반복된다 ⑦ 시간대별 반복은 **정보로 인정**(장전 관망→장중 급등→마감 서킷브레이커는 같은 뿌리의 다른 정보), 완전 동일만 프롬프트로 방지. **핵심 발견 — 메일은 안 무거워진다:** 일일 메일은 07:40 발송이라 **장전 행만** 담긴다(장중 13:00·마감 19:00은 그 뒤 기록). 즉 행을 늘려도 웹은 하루치가 쌓이고 메일은 아침 분량만 는다. **BSL(`90382fc`, 배포 완료):** 정렬 버그 수정 — 날짜만 비교해 같은 날짜는 비교값 0이라 시트 입력 순서(장전→장중→마감)가 남아 **날짜끼리는 최신순인데 하루 안에서는 오래된 순**이었다(하루 1~2건일 때는 안 보임). `mktPeriodRank`/`mktDailySort` 추가, cap 15→**45**(카테고리당 하루 최대 9행 기준 약 5일분). **텔레그램(`notifier/sheets.py`, 코드·검증 완료·배포 대기):** `market_prompt` 배열 요청·180자·시간대 중복 회피 / **`parse_signals` 신규**(배열 파싱 + **항목별 §6 필터** — 종전 `parse_signal`은 1건 위반 시 카테고리 전체를 버렸다 + 제목 중복 제거 + `MARKET_MAX_ITEMS=3` cap) / `write_market_daily` 항목당 1행. **검증:** BSL JS 스위트 8개 + Python 29건, 텔레그램 **pytest 35건**(신규 13건 포함) 전부 통과, `parse_signal` 잔존 참조 0건. **⚠️ 배포:** `deploy/deploy.ps1` 실행만 하면 된다(업로드 목록에 `notifier/sheets.py` 포함). **서버 재시작 불필요** — cron이 매번 `summarizer.py`를 새로 띄우는 구조라 파일만 바뀌면 다음 실행부터 적용된다(재시작이 필요한 건 FastAPI 대시보드 서비스뿐, 이번 변경과 무관). 새 환경변수 없음. **⚠️ 텔레그램 폴더는 git 저장소가 아니다** — 되돌릴 수단은 `notifier/sheets.py.bak2` 뿐. **배포 후 확인:** 로그 `[sheets] 경제 장전: 3행` 형태 / `시장-일일`에 시간대당 최대 9행 / 웹 시장 탭에서 같은 날 **마감이 위**로 / 07:40 메일이 3건→최대 9건. 분량 조정은 `MARKET_MAX_ITEMS`(개수)와 프롬프트 `2~3문장`(길이)으로 한다.
- **다음 운영 확인:** ① 내일 아침 일일 시황 메일이 콘솔에 지정한 시각(±15분)에 도착하는지 — 장전 단일가(08:30) 전인지 확인. ② 다음 일요일 06:00 초안 로그에서 `[weekly] 초안 0건 분야` 줄로 bio·macro·flows가 채워졌는지, 같은 기사 반복이 사라졌는지 확인(`[dedup]` 제외 건수 로그). ③ 다음 일요일 06:00 초안에서 `원문제목·원문일시·수집일시·생성엔진`이 채워지는지 확인하고, 화요일 17:05 자동 평가와 20:00 발송 이력을 점검한다. W31은 이미 `emailed`이므로 `sendWeekly()`를 다시 수동 실행하지 않는다. Actions의 `actions/checkout@v4` Node.js 20 폐기 예정 경고는 현재 실행에 영향이 없으며 후속 유지보수에서 갱신한다.
- **★★★★ 현재 요약(2026-07-21 · 여기부터 읽기):** 운영자 관리자 콘솔(**BSL_admin** standalone Apps Script, 액세스: 나만) 배포·라이브. 4모듈(①주간 승인 ②대시보드 수동 ③서재 업로드 ④방문 통계) 전부 동작 + 주간 승인 **전체선택** + 서재 **수정/삭제**. bio 앵커 라이브(기술 5분야: ai-infra·semicon·power·space·bio). 모듈②(대시보드 수동)·파이프 토글 둘 다 Secret 등록·end-to-end 활성(`DASH_MANUAL_CSV`·`SETTINGS_CSV`). 서재 IonQ 리포트 게시. **완료 = Phase 1 + 콘솔 전체.** **운영(매주) = 콘솔에서 발행주 한 주로 통일 승인 + 옛 주 draft 삭제.** **다음 관문 = 커스텀 도메인 연결**(순서: 콘솔 ✅ → 도메인 → 유료 이전). 이후 Phase = 유료 사이트(결제·로그인·회원 DB)·텔레그램 서버 토글 연동. ⚠️ **콘솔 코드(admin/) 변경은 Apps Script 재붙여넣기+재배포 필요**(public/처럼 자동배포 아님). 상세 이력은 아래 날짜별 항목.
- **★★★ 주간 발행 파이프라인 완성·라이브(2026-07-16 세션):** 기술(ai-infra·semicon·power·space)+금융(kr-equity·us-equity·bond·commodity)+경제(macro) 전부 반자동 파이프 연결·프로덕션 검증. **로봇(월 06:00 KST cron)→`주간-초안` 시트 draft→사용자 approve→사이트 자동 표시(코드 수정 0).** 발행주 라벨="2026년 29주 (7월 13일)". 렌더러=헤드라이너 선택적+신규도메인+빈상태 "준비 중". robot DOMAINS=11(기술5·금융4·경제1, flows 포함), 사이트 live 분야=기술4·금융4·경제1. 상세=아래 "다음에 할 일" 3번. **잔여(사용자):** us-equity·bond·commodity·kr-equity·macro draft 선별 + `주간-초안` 시트 정리(검증차 반복 실행으로 W29 draft ~100행 누적 — approve 후 나머지 draft 삭제). bio·space·flows는 신호 쌓이면 개방.
- **★ 발행주 혼재 방지 — "이번 호 주(issue week)" 정렬(2026-07-20):** 렌더러가 도메인별로 각자 최신 주를 표시하던 것이 원인이 되어 한 카테고리 안에서 발행주가 혼재(예: 기술 semicon만 W30, 나머지 W29)될 수 있던 문제를, `categoryIssueWeek`(카테고리 도메인 중 승인된 **최신 발행주**) 기준으로 전 도메인을 한 주로 정렬해 해결. **그 주 승인분만 표시, 그 주 미승인 도메인은 "준비 중"(옛 주 안 섞음), 매주 자동 롤오버.** `weeklyBestDomain`=콘텐츠 있는 도메인을 기본 탭으로. 시트 전무 카테고리는 정적 폴백. **이게 정식 공개의 전제**(주간지처럼 한 호=한 주). **운영 규율:** 매주 한 주 배치를 통으로 승인하고 발행 후 옛 주 draft 삭제. **⚠️ 현재 데이터:** 검증 반복으로 W29·W30 혼재 → 정렬 규칙상 카테고리별 최신 주(대개 W30)만 뜨고 나머지는 "준비 중". **사용자 즉시 조치:** 각 카테고리 도메인을 한 주로 통일 승인(오늘=W30 초안 있으니 W30로 통일 권장) + 옛 주 draft 삭제.
- **방문자 수 측정 — 구현·라이브(2026-07-20):** 자가소유형 비콘 채택(데이터 자가 소유·향후 관리자 콘솔 전조). `analytics/Code.gs`(Apps Script 웹앱: `방문로그` 탭 append + `?count=1[&callback]`→JSONP `{views,visitors}`) + `script.js` logVisit(이미지 비콘, 쿠키·PII 없음, localStorage 익명ID) / loadVisitCount(JSONP로 CORS 우회) / renderVisits + `index.html` 푸터 카운터([data-visits], 저작권 옆, 메인만). `site.js` VISITS_WEBAPP_URL 연결(별도 **BSL_analytics** 시트 — 방문로그는 쓰기 웹앱 필요라 BSL_market과 분리). 카운터=페이지뷰(views), 순방문(visitors) 전환은 1줄(d.views→d.visitors). 검증: JSONP `{"views":N,"visitors":M}` 확인. **잔여:** `방문로그`의 `/beacon-test` 테스트 행 삭제.
- **서재(Library) 시트 업로드 경로 — 구현·라이브(2026-07-20, d 자동화 보완):** 에디터가 git 커밋 없이 **시트 행 추가로 리포트 업로드**. `site.js` LIBRARY_SHEET_CSV(BSL_market **'서재' 탭** gid=1244768960 게시 CSV 연결) + `script.js` parseLibSheet(본문 md를 셀에 인라인) / fetchLibraryItems(library.json .md와 병합·발행일 내림차순·id중복 시트우선) → loadLibrary/loadReport 공용. read.html은 시트 항목이면 .md fetch 없이 셀 본문 렌더. 시트 열: `id·유형(report/note)·분류·발행일·제목·요약·태그·본문·access`. **유형=note면 '운영자의 서가'(개인 노트), report면 심층 리포트 그리드.** 기존 .md 리포트(CPO·우주SCM·책노트) 무손상(시트 0행이면 기존만). **잔여:** 사용자가 첫 리포트 행 추가(IonQ 에너지 리포트 정리본 준비됨 = `C:\Users\paunj\Downloads\IonQ_Energy_Advantage_Report_clean.md`, [cite:1]·H1 제거 완료; 메타 9열 값은 대화에 정리됨).
- **자동화 현황 & a/c/d 검토(2026-07-20):** a(주간 기술·금융·경제)=반자동(로봇→시트 approve→사이트). b(시장 텔레그램)=자동. c(대시보드)=자동(평일 cron, 수동은 DRAM 계약가 카드 분기 1회뿐). d(서재)=시트 업로드 경로로 **보완 완료**. e(경제캘린더)=반자동(별도 앱→시트). **통합 결론:** a·c(수동카드)·d·방문통계는 정식 웹사이트의 **'운영자 관리자 콘솔'**(초안 승인 UI + 대시보드 편집 + 서재 업로드 + 방문 통계 + 백엔드/인증/DB)로 수렴 = Phase 2 핵심 산출물. 그 전까진 시트+Apps Script 임시 스택 유지.
- **★ 관리자 콘솔(Phase 2) 기획 확정(2026-07-20) — [docs/admin-console-plan.md](admin-console-plan.md):** 3결정=①Apps Script+Sheets 유지(₩0, 임시 스택) ②운영자 1인 범위(결제·회원 이후 Phase) ③인증=웹앱 "액세스: 나만"(Google 로그인=인증, TOKEN 불필요). 형태=신규 **BSL_admin** standalone Apps Script가 HtmlService 콘솔 SPA 서빙, `openById`로 BSL_market·BSL_analytics 접근. 4모듈=①주간 초안 승인(주간-초안 탭, W29/W30 혼재 정리 정조준) ②대시보드 수동 카드(valuechain_manual.json→**대시보드-수동 탭 신설**, 이 모듈만 dashboard.js 소형 변경) ③서재 업로드 폼(서재 탭) ④방문 통계(방문로그 읽기). 파이프 토글(briefing_enabled) 노출. 웹 게시 순서=콘솔 배포→커스텀 도메인→유료 이전 준비.
- **★★ 관리자 콘솔 구현 완료(2026-07-20) — 코드 전부 작성·문법/기능 검증 통과. 상세=[docs/admin-console-plan.md](admin-console-plan.md) "구현 완료" + [admin/README.md](../admin/README.md):** 산출물 `admin/Code.gs`(인증 게이트=`Session.getActiveUser` allowlist + 4모듈 서버함수 + 토글) · `admin/index.html`(HtmlService SPA, DESIGN.md 토큰, 4탭 + 상단 파이프 토글, `google.script.run`) · `admin/README.md`(설치·배포 안내). **모듈 ②는 dashboard.js 무변경 채택** — `fetch_dashboard.py`가 `대시보드-수동` 탭 CSV(env `DASH_MANUAL_CSV`)→`valuechain_manual.json` 재생성(미설정 시 기존 보존, fail-safe), dashboard-data.yml에 env+`git add valuechain_manual.json` 추가. **토글=실제 동작**(가짜 아님): `settings` 탭 `pipeline_enabled`(콘솔 자동생성)를 `scripts/lib/toggle.py`가 읽어 `fetch_weekly.py`·`fetch_market.py` 진입부 early-exit(fail-open — 미설정/실패는 활성, `0`만 중지), weekly-draft/market-data.yml에 `SETTINGS_CSV` env 추가. ⚠️ **텔레그램 서버(별도 repo)는 아직 토글 미연동**(후속). **검증:** py_compile 4파일 + toggle 5케이스 + manual_from_sheet(2카드·스킵행·미설정 no-op) + node 문법(Code.gs·index.html JS) 전부 통과. **남은 것(사용자 셋업, admin/README.md):** ① `대시보드-수동` 탭 신설(헤더 카드키·라벨·단위·주기·출처·시각·값) ② BSL_admin Apps Script에 두 파일 붙여넣기 + 스크립트 속성(MARKET_ID·ANALYTICS_ID·ADMIN_EMAILS) + 배포(액세스: 나만) ③ `settings`·`대시보드-수동` 탭 CSV 게시 → Actions Secret `SETTINGS_CSV`·`DASH_MANUAL_CSV`. **다음: 사용자 배포 후 실데이터로 W29/W30 정리 검증 → 커스텀 도메인.**
- **★ 관리자 콘솔 배포 완료·정상 동작(2026-07-21):** 사용자가 BSL_admin Apps Script 배포(액세스: 나만) 완료. 콘솔 4모듈 동작, 승인→tech.html 라이브 자동 반영 확인. **bio 앵커 개방(2026-07-21):** 운영자 콘솔에 W30 bio 브리핑 승인분 확보 → `tech.js` bio `status: soon→live`. 리졸버(loadWeeklySheet)가 approved bio 행을 `분야="bio"`로 렌더(power/space와 동일 메커니즘). robot DOMAINS는 원래 bio 포함이라 변경 없음. **주의: 기술 카테고리는 categoryIssueWeek('이번 호 주')로 정렬 → bio가 W30이면 다른 기술 도메인도 W30 승인분이 있어야 함께 표시(아니면 '준비 중'). 콘솔로 주 통일 관리.** finance flows만 soon 남음. **추가 진행(2026-07-21):** ① 콘솔 ① 주간 승인 테이블에 **전체 선택 체크박스**(wToggleAll) 추가·동작(전체선택→뺄 항목 해제→선택 삭제). ② **모듈 ② 대시보드 수동 라이브 검증 완료** — 사용자가 BSL_market에 `대시보드-수동` 탭 신설(헤더 카드키·라벨·단위·주기·출처·시각·값) + DRAM 3점 이관 + CSV 웹게시 + Actions Secret `DASH_MANUAL_CSV` 등록. dashboard-data 워크플로 수동 실행 로그 `→ valuechain_manual.json (1 cards)` + 라이브 파일 note="운영자 콘솔이..." 확인 → **콘솔 시트 입력→크론 재생성→배포 파이프 end-to-end 작동**. 향후 분기 갱신=콘솔 ② 폼. ⚠️ **콘솔 코드(admin/) 변경은 Apps Script 재붙여넣기+재배포 필요**(public/처럼 자동배포 아님). **남은 것:** (a) 파이프 토글 활성화 — `settings` 탭(콘솔 자동생성) CSV 웹게시 → Secret `SETTINGS_CSV`(현재 미설정=fail-open 항상 실행). 텔레그램 서버 토글 연동은 별도 repo 후속. (b) 서재 첫 리포트(IonQ) 콘솔 ③ 업로드. (c) 커스텀 도메인 연결(웹 게시 순서 다음 관문).
- **★ 콘솔 마무리 완료(2026-07-21):** (a) **파이프 토글 활성화 완료** — `settings` 탭 CSV 게시 + Secret `SETTINGS_CSV` 등록(모듈② `DASH_MANUAL_CSV`와 함께 확인). 이제 콘솔 🟢/🔴가 `fetch_weekly`·`fetch_market` 실행을 실제 제어(fail-open, `0`만 중지). 텔레그램 서버(별도 repo) 연동만 후속. (b) **서재 IonQ 리포트 게시 완료**(콘솔 ③, 유형=report·분류=기술). (c) **콘솔 ③ 서재 수정/삭제 기능 추가**(`libraryUpdate`=열별 병합 덮어쓰기 / `libraryDeleteRows` + 목록 액션열 수정·삭제, PR #5 병합). ⚠️ **서재 id 중복 주의(실제 발생):** 같은 id 두 행은 `fetchLibraryItems`가 dedup(byId 덮어쓰기=나중 행 우선)해 **한 카드로만** 표시되고 dedup 생존행 유형에 따라 리포트 그리드/노트 스트립 위치가 갈림. 리포트+노트 둘 다 원하면 id를 다르게 할 것. (IonQ 중복행 수동 정리 완료.) 게시 CSV는 콘솔 대비 수 분 캐시 지연 있음. **→ 남은 것 = 커스텀 도메인 연결뿐.**
- **★★ 커스텀 도메인 연결 완료(2026-07-21) — brevislab.com:** Cloudflare Registrar 구입 → DNS(A 4개 @→185.199.108~111.153 + www CNAME→sk-jeong-0513.github.io, **전부 DNS only/회색 구름**) → `public/CNAME`=brevislab.com(PR #9) → GitHub Settings→Pages Custom domain **수동 입력**(Actions 배포는 CNAME 파일로 자동 채워지지 않음) → Enforce HTTPS. 검증: apex/www 200, Let's Encrypt 인증서(CN=brevislab.com) 발급, www→apex 301. ⚠️ **프록시는 회색(DNS only) 유지**(주황이면 Pages HTTPS 막힘). **⑤ LLM 엔진 탭 추가**(주력 선택, settings.llm_primary, ai.py fail-open)도 완료(PR #7). **SEO/소셜 메타 추가(PR 예정):** 9개 페이지에 canonical + og:*(title/description/url/type/site_name/locale) + twitter:card=summary(기존 title/description 재사용, url=brevislab.com). **og:image=`public/assets/og.png`**(1200×630 브랜드 카드, 시그널라인 스파크라인 모티프, Pillow+Malgun 생성, DESIGN 준수) 추가·twitter:card=summary_large_image. **금융 flows 앵커 개방(PR #8)**: 금융 live=5(flows 포함), soon 없음. **작업 폴더 정리:** .bak 17개 + 옛 `.handoff/*.txt` 3개(gitignore) 삭제 — 현행 문서는 docs/HANDOFF.md. **→ 남은 마일스톤 = 유료 사이트 이전(결제·로그인·회원 DB)뿐.** 이후: 텔레그램 서버 토글 연동.
- **완료:** 랜딩 + 기술/금융/경제 페이지(각 샘플 4건 + 유료 키워드 칩) + 경제 캘린더(격자/리스트·필터). 데이터 주도 구조. 헤더 nav 정리("오늘의 브리핑"→#samples, 기술/금융/경제→각 페이지). 구독은 Google Form 사전채움(무료/유료). 경제 캘린더는 구글 시트(CSV) 실시간 연동 + 중복 방어.
- **기술 브리핑 콘텐츠 전략(2026-07-03 확정):** 독자=투자자 겸 기술 관찰자(삼성·하이닉스·미국 성장주 투자, 기술을 돈 흐름·병목으로 읽음). 차별화=선행 신호(1차 소스에서 경제지 헤드라인보다 먼저)+메커니즘→밸류체인 연결. 주기=주간. 구조=분야(밸류체인 테마) 메뉴, 티어=선택 분야 수(저가 1/고가 2~3). 런칭 앵커 2개=AI 인프라(시스템 계층: 가속기 시스템·서버·DC·네트워크·전력·추론)+반도체 공급망(물리 계층: 칩~패키지·소재·장비·파운드리), 경계=물리 vs 시스템. 로드맵 3개(전력·에너지/우주·방산/바이오)는 수요 보고 순차 개방(전력 개방 시 AI 인프라에서 전력 접점 이사). 이슈 그릇=다이제스트(선행 신호 3~5개)+헤드라이너 딥다이브 1개. 무료=신호 리스트(제목+1줄)+헤드라이너 요약 / 유료=각 신호 전문+딥다이브+종목·근거+아카이브. 소싱=완전 수동 시작(2~3호)→검증 후 반자동. 투자 수위=밸류체인 기업 명시 O, 투자판단(매수·매도·목표가) X + 면책.
- **약속 문장:** "기술을 돈의 흐름으로 읽는 투자자에게, 매주 1차 소스에서 경제지보다 먼저 잡은 선행 신호를, 어느 밸류체인 기업으로 이어지는가까지 연결해 주는 브리핑."
- **현재 상태(2026-07-07):** 기술 주간·분야 전환 + 앵커 1호 + 경제·금융 확장 + 3-카테고리 통합 메일러(수신동의 7명 실발송) + 지표 대시보드(P1·P2·P3) 전부 라이브 완료. 대시보드=6페어(금리·TGA·달러·구리·EWY·수출)+밸류체인, 자동 파이프라인(Yahoo·미 재무부·관세청 수출 API). 
- **★ 현재 상태(2026-07-08):** 위 대시보드 3건 + **서재(Library) 재구축 라이브**(8번) + **시장(주식) 종목 브리핑 무인 자동 발행 라이브**(9번) 완료. 시장=AI(deepseek→gemini)+§6 린트가 매일 자동 생성. nav=기술·경제·금융·시장·대시보드·서재·경제캘린더.
- **★★ 일일 파이프 재구성 완료(2026-07-16 확인, 금주 내내 정상·엔드투엔드 검증). 상세=[docs/market-plan.md](market-plan.md) "C단계 이후 재구성":** 텔레그램(오라클 Always Free 서버 `!03. NextProject/Telegram 자동화`) → 시장-일일/시장-본문 시트 → BSL 일일 메일. **Stage 0~5 전부 완료** + **LLM 시황 생성(갈무리)**(제목=`[장전] 헤드라인`, §6 린트, 시장 무관 카테고리 생략) + **상세 브리핑 첨부**(§6 마스킹 full_summary→시장-본문 탭→메일 하단 "상세 브리핑") + **잘림 수정**(MAX_OUTPUT_TOKENS 4096) + **Stage 5=텔레그램 자체 메일 OFF**(summarizer.py send_email 제거). 실배포 이슈 해결: deepseek 키 교체(sk-36f3e…), 살아있는 웹앱 URL(AKfycbyfK0…/exec), .env 브래킷 제거. **⚠️ briefing 토글 = 파이프 전체 스위치. 끄면 BSL 메일도 멈춤 → 계속 🟢 ON. 텔레그램 서버는 BSL 엔진이라 유지 필수.**
- **★ 남은 마무리 1건(사용자):** GitHub Actions `MARKET_WEBAPP_URL` 시크릿을 살아있는 URL(AKfycbyfK0…/exec)로 갱신 — 죽은 URL이면 시장-종목(GitHub) 파이프도 404. GitHub Secret은 어시스턴트 편집 불가.
- **★ 향후 과제(핸드오프 후 사용자 최종 마무리 — 현재 미지정/보류):** ① **주간 브리핑(기술/금융/경제 앵커) 자동화 안 됨** — 현재 수동 발행. ② **서재(Library) 업로드 방법 미지정** — 정적 호스팅이라 쓰기 경로(GitHub API+프록시+인증 or Sheets/Apps Script 인프라 재사용) 필요. ③ **유료 사이트 마이그레이션 미지정** — 결제·로그인·회원 DB(Phase 2). ④ **주식 브리핑에 기존 IonQ 웹 수준 기능 추가 안 됨.** ⑤ **추가 기능 전반 보류.**
- **★ C단계 완료(2026-07-09):** Telegram 서버(`!03. NextProject/Telegram 자동화`)가 일일 요약을 BrieSigLab `시장-일일` 탭에 파이프. 추가 파일: `notifier/sheets.py`(웹앱 POST, `tab:"시장-일일"`, KST 날짜) + `notifier/guard.py`(§6 린트 사본, source=BrieSigLab/scripts/lib/guard.py). **생성 아님·추출**: 제목=`시장 브리핑 · 오전/오후/저녁` 고정, 한줄=요약에서 §6 통과 첫 실질 라인(전부 위반이면 write skip+로그). `summarizer.py run()`에서 MD 아카이브 직후 호출(try/except, MARKET_WEBAPP_URL 없으면 no-op). BrieSigLab `script.js` 시장-일일 렌더러=최신순 정렬+상위 15개 cap 추가(무한 누적 방지). config.py/.env.example에 `MARKET_WEBAPP_URL`/`MARKET_WEBAPP_TOKEN` 추가. **남은 사용자 셋업:** (a) 시트에 `시장-일일` 탭 헤더 정확히 `날짜·제목·한줄·출처URL·access` 생성, (b) Telegram 서버 `.env`에 MARKET_WEBAPP_URL/TOKEN(=BrieSigLab Actions Secret 동일값) 입력. **주의:** Telegram 채널이 시장 지향이어야 한줄이 시장 신호로 읽힘(일반 뉴스면 §6는 안전하나 "시장 브리핑" 라벨이 과함) — 채널 성격 확인 필요. **Stop=기존 briefing_enabled 토글이 전체 run 차단(별도 토글 불필요).**
- **★ 일일 파이프 재구성 Stage 0~4 완료(2026-07-09), 상세=[docs/market-plan.md](market-plan.md) "C단계 이후 재구성":** 4결정=①일일메일=BSL Apps Script ②Telegram 자체메일 OFF ③시장 탭 경제/금융/기술 서브탭 ④주기 장전·장중·마감. **Stage 1**(카테고리 파이프: group_summaries 경제/금융/IT기술→기술, 카테고리당 1행+분류 컬럼) **Stage 2**(market.html 서브탭 UI+script.js 분류 필터) **Stage 3**(장전/장중/마감 라벨+프롬프트 시황 힌트) **Stage 4**(mailer/Code.gs sendDailyMarket+createDailyTrigger, 시장-일일 openById, 동의 전체 발송, 아침 1회). **Stage 5(완료 2026-07-09)**=Telegram send_email 제거. **남은 사용자 셋업:** (a) TG 서버 deploy.ps1+`.env`(MARKET_WEBAPP_URL/TOKEN)+restart, (b) 시장 시트 `시장-일일`에 `분류` 컬럼, (c) BSL main push 배포, (d) mailer CFG.MARKET_SHEET_ID+웹앱 재배포+TEST_MODE 미리보기+createDailyTrigger().
- **★ (전부 해결됨 2026-07-16):** 상세 브리핑 **잘림** = 원인=텔레그램 full_summary 생성 토큰 상한 → `MAX_OUTPUT_TOKENS 2048→4096`으로 해소. 카테고리 **중복** = 실운영이 하루 1회라 비발생(당시엔 테스트 반복 탓). 📌 이모지 정상. **Stage 5 완료**, 메일러 실운영(TEST_MODE=false+createDailyTrigger) 가동. → 메일 dedup은 불필요로 판단(미적용).
- **★ 라이브 확인(2026-07-09):** 시장-일일 파이프 **엔드투엔드 정상 동작 검증**. 서버 배포+`.env`(deepseek 키 sk-36f3e…로 교체, MARKET_WEBAPP_URL 살아있는 AKfycbyfK0…/exec) 완료, 시장-일일에 `분류` 컬럼 추가됨, BSL 사이트 배포로 시장 탭 서브탭 표시 확인. **한줄 추출→LLM 시황 생성(갈무리)로 교체** — `sheets.write_market_daily(summarize, cat_summaries, period)` async, 카테고리별 `{제목,한줄}` 생성+§6 린트, 시장 무관 카테고리 생략, 제목=`[장전/장중/마감] 헤드라인`. 실산출 예: "경제 [장전] 반도체株 동반 강세 / 기술 [장전] 삼성 파운드리 흑자 전환 신호". **남은 것(2026-07-16 기준): (2)(3)(4) 완료. (1) GitHub Actions `MARKET_WEBAPP_URL` 시크릿 갱신만 남음.**
- **★ 시장 파이프라인 정리 항목:** `시장-종목` 탭 **B(티커)열 텍스트 서식**(현재 앞자리 0 떨어짐, 표시용) · 시장-종목 오래된 행 pruning(append 누적이나 사이트는 종목별 최신 1건 dedup으로 무해) · Google News 간헐 503(재시도로 완화, 나쁜 날엔 0행이라 사이트는 직전 유지).
- **★ 완료 상세(2026-07-08):** ① 티커=현재값 확정(BASKET·KRSEMI_OPTIONS: 삼성·하이닉스·한미·주성·리노 + 동일가중 바스켓, 코드변경 없음). ② HBM 카드→DRAM 계약가 지수로 용도변경 — TrendForce DRAM 스팟가 차트는 사용불가(유료·API없음, DDR5이지 HBM아님)로 판정, HBM은 스팟거래 없어 공개가격 부재. 대신 `valuechain_manual.json`을 TrendForce 분기 계약가 상승폭(QoQ, 1Q26 +90~95%·2Q26 +58~63%)을 직전분기=100으로 지수화한 실데이터로 교체(v=[100,192,307], 출처 note 표기). dashboard.js `vcCard`에 `period`/QoQ 변동 지원 추가 + 태그 "수동·샘플"→"수동". **에디터는 매 분기 발표 시 t/v 한 점씩 추가.** ③ 랜딩 `index.html` #dashboard: 가짜 6카드(INDICATORS)+"유료 구독자 제공" 오카피 제거 → 무료 대시보드 설명 카피 + `dashboard.html` CTA 버튼. 고아 코드 정리(INDICATORS 상수·renderIndicators·`.ind*` CSS·sampleNote/locked i18n). `.ind-grid`/`.sample-note`는 dashboard.html에서 재사용 중이라 유지.
- **★ 제약 메모(대시보드):** data.go.kr 인증키를 **재발급하면 활성화까지 수 분~수십 분** 걸려 그 사이 수출 API가 403을 반환함(정상). 키는 Actions Secret `DATA_GO_KR_KEY`. 파이프라인은 **이번 수집에서 빠진 시리즈를 직전 dashboard.json에서 보존**하므로 일시 실패로 페어가 사라지지 않음. 데이터 워크플로는 GITHUB_TOKEN 푸시가 배포를 자동 트리거 못 해 `gh workflow run deploy-pages.yml`로 명시 트리거함.
- **기술 페이지 구조 변경 완료(2026-07-06):** tech.js를 주간·분야 모델로 재작성(`TECH_DOMAINS` 5개=live 2+soon 3, `TECH_WEEKLY` 분야별 이슈={signals[] 다이제스트 + headliner 딥다이브}). `BRIEFINGS_TECH`는 헤드라이너에서 파생해 랜딩 계약 유지(무손상). tech.html=분야 메뉴(`data-tech-menu`)+주간 이슈(`data-tech-weekly`), 유료 섹션은 분야 티어 카피로 정합화. script.js=`techState`(module-level)+`renderTechWeekly`+`data-tech-domain` 위임 핸들러(전역 lang 리스너와 분리). style.css=`.tech-chip`/`.sig`/`.deep-lock` 등 신규(기존 토큰만). 검증: node 문법+파생+렌더 로직(분야 토글·KO/EN) 통과. 유료 body는 표시용 잠금(Phase 1, 실제 페이월 없음).
- **앵커 2개 1호 작성 완료(2026-07-06):** 공개 출처 리서치 기반 초안. AI 인프라=신호 4(capex 부품가·CPO 상용화·전력냉각 병목·자체칩) + 헤드라이너 "CPO, AI 네트워크 병목을 광으로". 반도체=신호 4(HBM4 본딩 분기·SK하이닉스 미국 후공정·유리기판 상업출하·CoWoS 캐파병목) + 헤드라이너 "HBM4, 병목은 셀이 아니라 본딩". 밸류체인 기업 실명 반영(엔비디아·브로드컴·TSMC·삼성·SK하이닉스·인텔·SK Absolics 등, 관찰·면책). 출처: SemiAnalysis/IDTechEx/TrendForce/EE Times/Tom's Hardware/CNBC. **주의: 배지·푸터의 "샘플" 문구는 실내용과 불일치 — 정식 오픈 시 tech용 배지를 "리서치 초안/검증본"으로 교체 권장(전역 문구라 미변경).**

## 다음에 할 일 (우선순위)

1. ~~기술 페이지 구조 변경~~ **완료(2026-07-06).**
2. ~~앵커 2개 1호 작성~~ **완료(2026-07-06, 리서치 초안).**
3. **주간 발행 파이프라인(반자동) — ①②③ 자동화 완료(2026-07-16, 시장 파이프 복제).** 아래 5단계 확정안 중 수집·초안·시트append를 구현. 신규: `scripts/fetch_weekly.py`(도메인별 Google News RSS → LLM 후보 신호 카드만 생성, 자동요약 금지 → §6 린트 → dry-run/POST) + `.github/workflows/weekly-draft.yml`(월 06:00 KST cron, 외부 발송 없음, Stop=Disable workflow). **쓰기 웹앱=BSL_market 시장 웹앱 재사용**(market/Code.gs가 헤더로 임의 탭 매핑) — 별도 Apps Script 배포·신규 시크릿 불필요, tab='주간-초안'으로 POST. 워크플로가 `MARKET_WEBAPP_URL`/`MARKET_WEBAPP_TOKEN`을 그대로 재사용(분리 시 WEEKLY_* 시크릿으로 교체). **도메인=가동 앵커 2개(ai-infra·semicon)**, `DOMAINS`에 항목 append로 로드맵/타 카테고리 확장. **arXiv 어댑터(feed_arxiv)는 코드에 유지하나 기본 미사용** — `all:` 검색이 최신순으로 무관 논문(천체물리·Salesforce 등)을 섞어 노이즈. 재투입 시 `cat:` 필터로 좁힐 것. 검증: py_compile + 오프라인 단위테스트(JSON파싱·행매핑·§6 드롭·발행주 라벨) + 실 Google News 수집 dry-run 통과. LLM 생성·웹앱 POST 구간은 키/시트 의존(시장 파이프와 동일 구조). **'주간-초안' 탭 헤더 정확히:** `분야·발행주·유형·제목ko·제목en·한줄ko·한줄en·밸류체인·출처URL·선행도·status`. **남은 셋업(사용자, 최소):** (a) **BSL_market**에 '주간-초안' 탭 추가 후 첫 행 헤더를 위와 정확히 동일하게 입력, (b) 끝 — 시장 웹앱·시크릿 재사용이라 새 배포/시크릿 없음, (c) `python3 scripts/fetch_weekly.py --dry`로 카드 품질 확인 후 cron 활성(현재 미검증 구간=LLM 생성·POST). **⑤ 발행 라이브 + 파이프라인 전체 프로덕션 검증 완료(2026-07-16, 전부 커밋·배포).** Actions 워크플로 실제 실행 → `[write] POST 200 added:10/20` 확인(시크릿·웹앱 URL 살아있음). **한 바퀴 검증:** ①로봇이 `주간-초안`에 draft 기록 → ②사람이 시트 status draft→approved → ③사이트가 approved 신호 자동 표시. `WEEKLY_SHEET_CSV`(tech.js)=주간-초안 게시 CSV(gid=530467230) 연결. **렌더러 확장(script.js):** `weeklyResolveIssue`/`weeklyIssueHtml`가 헤드라이너 선택적(유형=headliner 승인행도 읽음, 없으면 신호만) + 정적 base 없는 신규 도메인 렌더 + live-빈 도메인 "준비 중"(soonBody 아님) + 발행주 라벨 "2026-W29"→"2026년 29주". 정적 도메인 출력 동일(회귀 없음). **로드맵 개방:** power·space **live**(로봇 신호 5건씩 확보), **bio는 soon 유지**(로봇 초안 0건 — 빈 라이브 대신 곧, 콘텐츠 쌓이면 live). robot `DOMAINS`=5개(ai-infra·semicon·power·space·bio). power/space는 시트에 draft 5건씩 대기 → 사용자 approve 시 표시. **주간 사이클(코드 수정 없음):** 월 06:00 KST cron이 draft 기록 → 사용자가 시트에서 approve → 사이트 갱신. **전략 메모:** 시트 선별(④)은 Phase 1 임시 — 웹서비스 이전 시 관리자 화면으로 대체(별개 작업). **금융·경제도 파이프 연결·검증 완료(2026-07-16):** `WEEKLY_SHEET_CSV`를 tech.js→site.js로 이동(tech/finance/economy 공용). script.js `weeklyResolveSingle` 추가로 경제 single 모드도 시트화(sheetDomain="macro"). fetch_weekly.py에 kr-equity·us-equity(금융)·macro(경제) DOMAINS + **카테고리별 `hint`**(tech="밸류체인 병목" 기본 / 금융=수급·실적·밸류에이션 / 경제=금리·물가·정책). **원인 규명:** tech 전용 프롬프트라 금융·경제 LLM이 []만 반환 → hint로 해소. 검증 실행: kr-equity 5·macro 4건 생성 확인. **로봇 DOMAINS=8**(ai-infra·semicon·power·space·bio·kr-equity·us-equity·macro). finance 분야 id는 finance.js FINANCE_DOMAINS와 일치. **⚠️ 주간-초안 시트 정리 필요:** 검증차 파이프를 여러 번 돌려 W29 draft가 대량 누적(중복) — 사용자가 draft 행 일괄 삭제 권장(사이트는 approved만 읽어 무해). 정상 주간 운영(주 1회 cron)은 중복 없음. **금융 분야 전체 개방(2026-07-16):** 로봇에 bond·commodity DOMAINS+hint 추가, 신호 확보(us-equity 5·bond 4·commodity 5) → finance.js에서 bond·commodity soon→live. 금융 live=4(kr-equity·us-equity·bond·commodity), flows만 soon. robot DOMAINS=10. **발행주 라벨 개선:** `weeklyWeekLabel`이 ISO주를 "2026년 29주 (7월 13일)"(그 주 월요일)로 표시(정적 폴백 라벨과 톤 통일). **⚠️ 주간-초안 시트 대량 누적:** 셋업·검증차 파이프를 여러 번 돌려 W29 draft가 ~100행 누적 — 사용자가 원하는 분야만 approve 후 나머지 draft 일괄 삭제 권장(사이트는 approved만 읽어 무해, 주 1회 정상운영은 중복 없음). **남은 선택지:** us-equity·bond·commodity·kr-equity·macro draft 선별(사용자) · 헤드라이너 시트 작성(유형=headliner, 렌더러 지원) · bio/space 신호 쌓인 뒤 · flows 개방.
4. **무료 구독자 메일링 — 메일러 완성·end-to-end 검증 완료(2026-07-06). 실전 발송(TEST_MODE=false)만 사용자 판단 대기.** 검증됨: 발송+렌더+개인화(관심 키워드)+토글/수신거부 링크 클릭→`관심분야(기술)` 시트 반영까지. 웹앱 배포·SALT 고정 완료. 경로=**Google Apps Script**(사이트 호스팅과 독립, 정식 도메인 이관 시 버릴 임시). 코드=`mailer/Code.gs`(+README). **2-시트 모델:** `설문지 응답 시트1`(신원·동의 원천, 읽기 전용: `이메일 주소`/`메일 수신 동의`/`관심 키워드`) + `관심분야(기술)`(선호도 상태, 읽기/쓰기, 헤더 자동생성: 이메일·관심 분야·상태·갱신). `sendWeekly()` 수동 발송 + `doGet` 웹앱이 이메일 링크→`관심분야(기술)`에 upsert(분야 토글/수신거부). 최초 구독자=가동분야 전체 seed, 이후 토글로 좁힘. 수신거부는 응답 시트 안 건드리고 pref 상태=수신거부. **남은 것(사용자):** 시트에서 Apps Script 열기→Code.gs 붙여넣기(bound 아니면 SHEET_ID)→SALT/탭이름 확인→웹앱 배포→WEBAPP_URL 설정→TEST_MODE로 미리보기→false 전원. 매주=ISSUE만 갱신. GmailApp ~100/일. 정식은 도메인 인증 ESP로.
5. **로드맵 분야 개방** — 전력·에너지 / 우주·방산 / 바이오. 전력 개방 시 AI 인프라에서 전력 접점 이사.
   - **경제·금융 샘플 배포 완료(2026-07-06).** 렌더 제네릭화(`renderWeekly(cfg)`가 tech/finance/economy 공용). **금융**=자산군 분야 모델(FINANCE_DOMAINS: 국내 증시·미국 증시 live + 채권/원자재/펀드 soon, FINANCE_WEEKLY). **경제**=단일 매크로 다이제스트(ECONOMY_WEEKLY, 분야 메뉴 없음). 경계=경제(매크로·정책)/금융(자산·시장)/기술(산업 메커니즘). 콘텐츠는 "샘플" 배지. 랜딩 티저도 각 헤드라이너에서 파생. 메일러는 여전히 기술 전용. **주의: `renderCategoryFeeds`/`renderTopicChips`/`TOPICS`는 코드에서 제거 완료(2026-07-20 검증, `public/` 참조 0건 — 현행 렌더는 `renderWeekly(cfg)` 공용). 라인 49 참조.**
6. 유료 폼 문항(관심 분야 선택 = 티어 매핑) 추가 — 사용자 몫. 대시보드 실데이터, 커스텀 도메인은 선택.
8. ~~**서재(Library) 재구축**~~ **완료·라이브(2026-07-08).** [library.html](../public/library.html)(필터+리포트 그리드+운영자 노트 스트립) + [read.html](../public/read.html)(`?r=id`, marked CDN 렌더+인라인 면책+선두 H1 제거). `.md`+프런트매터 주기 업로드: `scripts/build_library_manifest.py`(stdlib) + `.github/workflows/library-manifest.yml`가 `library.json` 자동 생성·배포. 렌더러는 script.js에 통합(캘린더 패턴). nav "서재"→library.html(6페이지), 랜딩 티저(최신3)+CTA, tech/finance/economy "심층 리포트" 스트립. 시드=CPO·우주 SCM 리포트 + 도서 노트(svg). §6 투자판단 문구 최소 완화+면책. 상세 [docs/library-plan.md](library-plan.md). **후속(선택):** 노트 캡션 개인화(notes/2026-07-book-bird-language.md), 라이브 픽셀 점검, 금융·경제 리포트 추가 시 스트립 자동 노출.
7. **무료 구독자 관심 분야 "weight" 반영(향후)** — 무료 구독자가 폼에서 고른 관심 분야를 집계해 주간 콘텐츠 우선순위/노출에 반영. 2~3호 발행하며 데이터 쌓인 뒤.
9. **시장(주식) 탭 + AI 브리핑 워크플로 — 스펙 확정(2026-07-08 grill), [docs/market-plan.md](market-plan.md) 참조.**
   - **진척(2026-07-08): ①§6 린트 `scripts/lib/guard.py` ②시장 탭 `market.html`+렌더러(구글 시트 CSV: 시장-일일 gid=0/시장-종목 gid=2102188761) ③nav 재정리 ④AI 엔진 `scripts/lib/ai.py`(deepseek→gemini, OpenAI호환) ⑤종목 수집기 `scripts/fetch_market.py`(종목설정/시드 → Google News RSS → AI 요약 JSON → §6 린트 → 웹앱 POST) ⑥쓰기 웹앱 `market/Code.gs`(Apps Script, 헤더매핑 append, TOKEN) — 전부 완료·검증. ⑦`market-data.yml` cron 평일 07:00 KST **자동 발행 라이브**. 렌더러=종목별 최신 1건.** 키 시크릿: `DATA_GO_DEEPSEEK`/`DATA_GO_GEMINI`(워크플로서 DEEPSEEK/GEMINI_API_KEY로 매핑) + `MARKET_WEBAPP_URL`/`MARKET_WEBAPP_TOKEN`. **Stop=Actions 워크플로 Disable.** **남은 것: ~~(C) Telegram 일일 파이프~~ 완료(2026-07-09, notifier/sheets.py+guard.py) → (D) 금융·경제 자동 교체 → (E) 기술 AI 초안 보조. 정리: 시트 티커열 텍스트 서식(005930), 시장-종목 오래된 행 pruning(현재 append 누적, 사이트는 dedup으로 무해), (선택) 종목설정 CSV 게시→MARKET_CONFIG_CSV.** AI 키 검증용 `market-ai-test.yml` 있음. C 하이브리드(IonQ=Actions 네이티브 재구축, Telegram=서버 유지+Sheet 파이프). 무인 완전자동 + **§6 결정적 린트**(코드가 유일 방어선) + AI 라벨. 엔진=deepseek 주력/Gemini 폴백(engine.py 이식). 종목=Sheet 설정형(시드 반도체 바스켓). Google Sheet 워크북(시장-일일/시장-종목/종목설정) CSV 렌더. 범위 단계적(시장→금융·경제 샘플 교체→기술 초안 보조). Nav=기술·경제·금융·시장·대시보드·서재·경제캘린더. **선행 셋업(사용자): DEEPSEEK/GEMINI 키 Actions Secret + Google Sheet + 쓰기 경로(Apps Script 웹앱 추천).** 다음: 시장 탭 스캐폴드 + §6 린트 모듈부터.

**메일러 3-카테고리 확장(2026-07-06):** `mailer/Code.gs`를 기술+금융+경제 **통합 1통** 발송으로 재작성. 시트=응답 1(신원·동의) + 선호도 3(관심분야(기술)/(금융)/(경제), 스키마 이메일·관심 분야·상태·갱신 자동생성). 구독자당 구독 카테고리·분야 섹션만 담아 발송, 최초는 각 카테고리 가동분야 전체 seed. 이메일 링크=분야 토글(c/d)+전역 수신거부. **재설치 주의: SALT는 기존 값 유지, doGet 변경으로 웹앱 새 버전 재배포 필요.** 발송은 사용자가 Apps Script에서 실행(어시스턴트는 실행 불가). dead code(renderCategoryFeeds/renderTopicChips/TOPICS) 제거 완료.

**완료(2026-07-06 추가분):** tech 헤드라이너 배지 "샘플"→"무료 공개"(techPage.freeBadge), 푸터 면책 마지막 문장을 tech=공개출처 관찰/그 외=샘플로 정확화, 푸터 이메일(paun.jeong@gmail.com) 5개 페이지 mailto 링크화. 금융/경제/대시보드는 tech 수요 검증 후 진행(사용자 확인). 1호 내용은 사용자가 "충분히 좋음"으로 승인(에디터 검증 완료).

## 지표 대시보드 (2026-07-07 P1 완성)

**조직 원리:** 관계·상관 오버레이(태그 클릭→두 지표 이중Y축 겹침) + 산업 밸류체인 지표. investing.com과 차별화=데이터 폭이 아니라 선별된 관계. **전부 무료(미끼), 수익화는 브리핑 구독.**
- **dashboard.html** + `assets/dashboard.js`(uPlot 이중축 렌더) + `assets/vendor/uPlot.*`(self-host, 50KB). nav "대시보드"를 dashboard.html로 재지정(전 페이지).
- **데이터: 자동 실데이터 파이프라인** — `scripts/fetch_dashboard.py`(무키: Yahoo v8 chart + 미 재무부 DTS) → `public/assets/data/dashboard.json`. `.github/workflows/dashboard-data.yml`(평일 21:30 UTC 크론, 커밋·푸시). stdlib만(pip 불필요).
- **P1 페어 4개(실데이터):** 금리(^TNX)↔S&P(^GSPC), 달러(DX-Y.NYB)↔KOSPI(^KS11), 구리(HG=F)↔금리, EWY↔반도체 바스켓(삼성·하이닉스·한미·주성·리노 동일가중, rebase100). 정렬=UTC 일 버킷.
- **TGA↔지수 페어:** 코드 있으나 재무부 API가 로컬 샌드박스에서 차단돼 미수집 → **Actions 첫 실행 때 채워질 예정(account_type 필터 확인 필요).**
- **차트:** 이중 Y축(좌 primary, 우 warning), 6M/1Y/3Y 토글. 상관계수 없음(향후).
- **P2 완성(2026-07-07):** ① 페어5 우축을 **드롭다운**으로(기본 삼성전자 + SK하이닉스·한미·주성·리노·동일가중 바스켓). ② **산업 밸류체인 지표 섹션** — 자동 프록시 카드(SOXX·SMH·MU·TSM·SOX, 최근추세+~1M%) + 수동 핵심(`valuechain_manual.json`의 HBM 스팟가격, **수동·샘플** 배지, 에디터 주간 갱신). ③ TGA도 정상(878pts). 데이터 워크플로가 GITHUB_TOKEN 푸시 후 배포 자동 트리거하도록 수정.
- **P3 수출 완료(2026-07-07):** 관세청 수출 주요국가별 10일 잠정치 API(`apis.data.go.kr/1220000/cntyMmUtPrviExpAcrs/getCntyMmUtPrviExpAcrs`, https). 필수 파라미터=`strtYymm`/`endYymm`(YYYYMM 6자리). 키=Actions Secret `DATA_GO_KR_KEY`(fetch 스텝 env로 매핑). 응답 item{priodYear,priodMon,priodDt("01~10"),itemUsdAmt00=총계·01~10=주요국}. **월별 총액(10일 3구간 합산) 66개월 실데이터** → `exports` series + **`export-krsemi`(수출↔반도체 종목) 6번째 페어**. ⚠️ 사용자가 실키를 채팅에 노출 → 재발급 권고함.
- **남은 것:** ~~① 티커 확정~~ **완료(2026-07-08).** ~~② 수동 지표 실수치·출처 교체~~ **완료(2026-07-08, DRAM 계약가 지수).** ~~③ 랜딩 #dashboard 티저 정리~~ **완료(2026-07-08).** ④ (선택) 상관계수 배지 ⑤ 수출은 월별(66점)이라 종목(일별) 오버레이 시 월 해상도 — 필요시 개선.

## 주간 발행 파이프라인 (2026-07-06 확정안)

별도 상시 서버 불필요 — GitHub Actions 크론 러너 + 구글시트(캘린더의 CSV 패턴 재사용). **사람 선별(④)이 유일한 발행 스위치.**

1. **수집** — Actions 주간 크론 + Python(IonQ 자산 재사용). 분야별 소스 어댑터. 1차 소스 우선(실적콜 트랜스크립트·컨퍼런스 IEDM/ISSCC/GTC·특허 RSS USPTO/KIPRIS·arXiv), 보조 전문매체 RSS(SemiAnalysis/TrendForce).
2. **초안** — LLM 체인(Gemini 우선/Claude 폴백, Telegram 자산 재사용)으로 raw→"제목+1줄+메커니즘 후보+밸류체인 후보+출처+선행도" 후보 카드까지만(자동요약 금지).
3. **정리** — 구글 시트 append. 컬럼: 분야·발행주·유형(signal/headliner)·제목ko/en·한줄ko/en·밸류체인·출처URL·선행도·status(draft).
4. **선별(사람)** — status draft→approved, 헤드라이너 지정, 선행도·각도 판단, 밸류체인 실명 확정, 투자판단 표현 제거. 핵심 게이트.
5. **발행** — approved 행만 site.js가 CSV로 읽어 렌더(loadCalSheet 복제). 무료/유료는 access 컬럼.

방어: keep-alive 워크플로우 + `permissions: contents: write` + `.gitattributes`, 시크릿은 Actions Secrets/`st.secrets`, CSV PUT 시 `sha` 캐싱, 미검증은 status로 격리, 투자판단 표현 린트.

## 결정/제약 메모

- Phase 1 = 결제·로그인·DB 없이 폼으로 수요 검증. 투자 조언 문구 금지(밸류체인 기업 명시는 OK, 매수·매도·목표가·수급 판단은 금지, 면책 필수).
- 기술 브리핑은 **주간**. 금융/경제 주기는 별도 결정(이번 grill 범위 밖 — 기술만 확정).
- 분야는 소싱 파이프라인 단위 = 독자가 원하는 게 고정 분야 밖이면 소싱이 무너짐. 그래서 분야 메뉴로 고정하고 티어=분야 수.
- 배포는 `main` push → GitHub Actions 자동. `public/`만 서빙.
- **Pages 배포 stuck 이슈(2026-07-06 발생·해결):** upload는 성공하나 마지막 "Deploy"만 "Deployment failed, try again later"로 지속 실패(7/3~7/5). 원인=코드/워크플로/전역장애 아님, **레포별 Pages 백엔드 상태가 막힘**. 해결=Pages 소스 토글로 리셋 — `gh api -X PUT repos/OWNER/REPO/pages -f source[branch]=main -f source[path]=/` (legacy로) 후 즉시 `-f build_type=workflow`로 복귀 → 재배포 트리거하면 성공. 재발 시 이 순서로. (Settings→Pages 소스 토글 UI와 동일 효과)
- 기존 IonQ/Telegram 자동화는 향후 "주식종목브리핑"용 — 현재 기술/금융/경제 브리핑엔 미사용.
- 캘린더 뷰토글에 `class="lang"` 쓰지 말 것(전역 KO/EN 리스너 하이재킹). `.cal-viewtoggle` 사용.

---

## 핸드오프 작성 템플릿 (복사해서 위 "현재 상태"를 갱신)

```
- 마지막 갱신: YYYY-MM-DD
- 작업 브랜치: feat/...
- 진행 중: (무엇을 어디까지 했는지 1~3줄)
- 다음 단계: (바로 이어서 할 것)
- 막힌 점/주의: (있으면)
```

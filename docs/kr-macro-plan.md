# 한국 매크로 섹션 — 설계서 (한국은행 ECOS)

> 작성 2026-09-06. **같은 날 구현 완료.** 페어 1개 + 카드 10장이 들어갔다.
> 이 문서의 값은 **전부 2026-09-06 실측**이다. 추정치가 아니다.
>
> 구현하면서 설계에서 바뀐 것 둘 — 투자자예탁금은 ECOS 시계열이 없어 빠졌고(§5-C),
> 국제수지의 증권투자 항목도 못 찾아 **증시 자금은 2장(외환보유액·경상수지)** 이 됐다.
> 외국인 자금 흐름은 일간 페어의 드롭다운(외국인 순매수)이 대신 커버한다.

## 1. 왜 하는가

대시보드가 미국 편중이다. 미국은 FRED 로 금리·통화량·준비금이 촘촘한데,
한국은 야후에서 긁을 수 있는 **가격만** 있다.

| 영역 | 현재 커버리지 |
|---|---|
| 미국 | 지수 5 · 금리 2/5/10Y · M2 · 은행준비금 · TGA · 원자재 4 · 섹터 ETF 11 |
| 한국 | KOSPI · 원/달러 · EWY · 삼성전자 · 거래량 · 관세청 수출 — **매크로 0** |

한국 기준금리도, 국고채 커브도, CPI 도, 경상수지도 없다. ECOS 가 이 구멍을 메운다.

## 2. 확정 설계

### 2-A. 관계 페어 1개 (일간)

```
좌축  한미 10년 금리차 = 국고채(10년) − 미 10년물        [파생 시리즈]
우축  KOSPI                                          [기본값]
드롭다운  KOSPI · 외국인 순매수(유가증권) · KOSDAQ        [전부 ECOS 802Y001]
```

**만기를 맞춘 이유.** 한국 벤치마크는 국고 3년이고 미국은 2년·10년이라 조합이 갈린다.
10년−10년은 만기가 정확히 대응해 기간프리미엄이 섞이지 않고, 외국인 채권자금·환율과의
관계가 가장 잘 알려져 있다. 히스토리도 깊다(국고 10년 2000-12-18~, DGS10 훨씬 김).

**우축을 KOSPI 로 둔 근거는 실측이다**(§3-C). 원/달러보다 금리차와 강하게 붙고,
기존 `dollar-kospi` 페어와 겹치지도 않는다.

⚠️ **드롭다운 옵션은 전부 ECOS `802Y001` 에서 받는다.** `fetch_dashboard.py:56` 이
"한 페어 안에서는 소스를 섞지 말 것 — 드롭다운을 바꿀 때마다 차트 끝 날짜가 달라진다"
고 정하고 있다. 세 옵션이 같은 표·같은 거래일 달력이면 이 규칙을 우회가 아니라 정면으로
만족한다. **KOSPI 를 야후 `ks11` 로 두면 안 된다** — 드롭다운 안에서 소스가 섞인다.

> 규칙의 범위에 대한 메모: 이 규칙은 **드롭다운 안**을 말한다. 좌우축이 서로 다른
> 소스인 것은 기존 페어도 그렇다(`tga-index` 미 재무부+야후, `m2-index` FRED+야후,
> `wti-rates` 야후+FRED). 그래서 좌축 FRED·우축 ECOS 자체는 문제가 아니다.

### 2-B. 카드 11장 (월간)

기존 `vcCard()` 를 재사용한다 — 밸류체인·섹터가 이미 같은 함수를 쓰고,
**`period` 인자를 이미 받는다**(`dashboard.js:97`).

| key | 카드 | mode | 2026-09-06 실측 |
|---|---|---|---|
| `kr_csi` | 소비자심리지수 | `baseline100` | 104.5 (2026.08) |
| `kr_bsi` | 기업경기실사(전산업) | `diff` | 77.0 (2026.08) |
| `kr_esi` | 경제심리지수 | `baseline100` | 99.4 (2026.08) |
| `kr_cpi` | 소비자물가지수 | `yoy` | 120.05 (2026.08) |
| `kr_cpi_core` | 근원물가(농산물·석유류 제외) | `yoy` | 118.42 (2026.08) |
| `kr_reserves` | 외환보유액 | `mom` | 442,281,314 천달러 (2026.08) |
| `kr_ca` | 경상수지 | `diff` | 42,078 백만달러 (2026.07) |
| `kr_leading` | 선행지수순환변동치 | `baseline100` | 104.2 (2026.07) |
| `kr_ip` | 전산업생산지수(원계열) | `yoy` | 117.9 (2026.07) |
| `kr_retail` | 소매판매액지수(불변) | `yoy` | 102.4 (2026.07) |

| mode | 뜻 | 표시 예 |
|---|---|---|
| `yoy` | 전년동월비 % | `+2.1% YoY` |
| `mom` | 전월비 % | `+3.4% 전월비` |
| `diff` | 전월 대비 절대차 | `+2 전월차` |
| `baseline100` | 기준선 100 대비 | `+4.5 기준선` |

**신선도 충돌은 형태로 푼다.** 기존 기준(`fetch_dashboard.py:52-56`)은 *차트* 오른쪽이
비는 걸 막으려던 것이고, 실제로 M2SL·TOTRESNS 가 "월별 + 발표지연 78일" 로 탈락한 전례가
있다. 월간 지표를 **차트가 아니라 카드 + `2026.08 기준` 라벨**로 내면 그 문제 자체가 없다.
섹터 카드가 이미 90개만 읽는 것과 같은 패턴이다.

### 2-C. 표시 기준을 지표별로 두는 이유

지표 유형마다 의미 있는 숫자가 다르다. 통일하면 어느 한쪽이 오독된다.

- **CPI 전월비 `+0.2%`** 는 한국 독자가 기대하는 숫자가 아니다. 전년동월비로 읽는다.
- **심리지수는 변화율이 아니라 100 기준선 대비 위치**가 핵심이다.
  `104.5 (+1.2%)` 보다 `104.5 · 기준선 +4.5` 가 정보다.
- **예탁금·외환보유액·경상수지**는 전월비가 자연스럽다.

시리즈 정의에 `mode` 필드를 두고 `vcCard()` 가 그걸 읽게 한다.

## 3. 실측치 (2026-09-06)

### 3-A. 신선도 구간

| 구간 | 지표 | 판정 |
|---|---|---|
| **일간 D-2** | 국고채 1·2·3·5·10·20·30·50년, 콜, CD(91), 통안(91/1Y/2Y), 회사채 AA-·BBB-, 기준금리, 원/달러·엔·유로·위안, KOSPI·KOSDAQ·외국인순매수 | 페어 가능 |
| 월간 D+~7일 | CPI·근원·생활물가(202608), 심리 3종(202608), 외환보유액(202608), 투자자예탁금(202608) | 카드 |
| 월간 D+~37일 | 생산·소매·고용·경상수지·선행지수·PPI·수출입물가·교역조건(202607) | 카드(경계) |
| 분기·연간 | GDP성장률·가계신용(2026Q2), 기업경영·지니계수(2024) | **탈락** |

### 3-B. 한미 10년 정렬 (2025-01-01 ~ 2026-09-06)

```
KR 거래일 409 · US 거래일 419 · 교집합 395  → 94.3% 보존
미 휴장(KR만) 14일 · 한 휴장(US만) 24일
최대 공백 8일 (2025-10-02 → 10-10, 추석)
스프레드 범위 −1.94 ~ −0.15 %p   최신 2026-09-03 −0.40 %p
```

**교집합(inner join)만 쓴다. forward-fill 하지 않는다** — 94.3% 가 남고, 결측 보간은
없는 데이터를 지어내는 쪽이다. uPlot 이 8일 공백은 선으로 잇는다.
스프레드 최신일은 **둘 중 느린 쪽(미국)에 맞춰 D-3** 가 된다.

### 3-C. 상관 실측

| 조합 | 수준상관 | 변화상관 |
|---|---|---|
| **한미금리차 ↔ KOSPI** | **+0.920** | **−0.194** |
| 한미금리차 ↔ 원/달러 | +0.563 | −0.071 |
| DXY ↔ KOSPI (기존 `dollar-kospi`) | −0.298 | −0.054 |
| 원/달러 ↔ KOSPI | +0.566 | −0.087 |

⚠️ **`+0.920` 을 문구로 내보내지 말 것.** 기간 내 금리차는 −1.94 → −0.40 으로 단조
축소됐고 KOSPI 는 단조 상승했다. 둘 다 추세가 있으면 수준상관은 자동으로 높다(허위상관).
변화상관 −0.194 가 실제 신호에 가깝고 그것도 약하다. **차트로 쓰는 건 문제없지만,
수치를 주장으로 쓰려면 추세 제거 후 다시 재야 한다.**

## 4. ECOS 코드

### 확정 (항목 코드까지)

| 표 | 주기 | 항목 | 코드 | 범위 |
|---|---|---|---|---|
| `817Y002` 시장금리 | D | 국고채(10년) | `010210000` | 20001218~ |
| | | 국고채(3년) | `010200000` | 19981113~ |
| | | 국고채(2년) | `010195000` | 20210310~ |
| | | 회사채(3년,AA-) | `010300000` | 19950103~ |
| | | 회사채(3년,BBB-) | `010320000` | 20000930~ |
| | | CD(91일) | `010502000` | 19950103~ |
| | | 콜금리(1일,전체) | `010101000` | 19950103~ |
| `802Y001` 주식시장 | D | KOSPI | `0001000` | 19950103~ |
| | | 외국인 순매수(유가증권) | `0030000` | 20030102~ |
| | | KOSDAQ | `0089000` | 20030102~ |
| | | 거래대금(유가증권) | `0088000` | 19950103~ |
| | | 시가총액(유가증권) | `0183000` | 20030102~ |
| `901Y010` CPI 특수분류 | M | 농산물및석유류제외 | `QB` | 197501~ |
| | | 식료품및에너지제외 | `DB` | 199001~ |

### 월간 카드 (전부 실제 호출로 검증됨)

| key | 표 | 항목 | 비고 |
|---|---|---|---|
| `kr_cpi` | `901Y009` | `0` | 총지수 |
| `kr_cpi_core` | `901Y010` | `QB` | 농산물및석유류제외(1975~) |
| `kr_csi` | `511Y002` | `FME` | 소비자심리지수(2008.7~) |
| `kr_bsi` | `512Y007` | `AA` × `99988` | 업황실적BSI × 전산업. **2차원** |
| `kr_esi` | `513Y001` | `E1000` | 원계열(`E2000` 은 순환변동치) |
| `kr_reserves` | `732Y001` | `99` | 합계(01 금 · 02 SDR · 03 IMF · 04 외환) |
| `kr_ca` | `301Y013` | `000000` | 경상수지 |
| `kr_leading` | `901Y067` | `I16E` | 선행지수순환변동치(`I16A` 는 선행종합지수) |
| `kr_ip` | `901Y033` | `A00` × `1` | **2차원.** `1`=원계열 · `2`=계절조정 |
| `kr_retail` | `901Y100` | `G0` × `T2` | **2차원.** `T1`=경상 · `T2`=불변 · `T3`=계절조정 |

⚠️ **`901Y098`(소매업태별)에는 총지수가 없다** — 백화점·대형마트 같은 업태만 있다.
소매 총지수는 `901Y100`(재별 및 상품군별)의 `G0` 다.

⚠️ **2차원 표에서 항목을 하나만 주면 시점당 여러 행이 온다.** `901Y033/A00` 만 주면
원계열(117.9)과 계절조정(120.2)이 같이 오고, 무엇이 담겼는지 모르게 된다.
`ecos_series()` 가 마지막 값을 취하되 **로그로 알린다**(조용히 절반을 버리지 않는다).

### 찾지 못한 것

- **투자자예탁금** — `802Y001`(일간)에도 `901Y014`(월간)에도 없다. 100대 지표에는
  월간 최신값으로 나오지만 시계열 API 경로를 못 찾았다. 금융투자협회 계열로 보인다.
- **증권투자(자산/부채)** — `301Y013` 항목 목록에서 `경상수지` 외에는 이름으로 안 잡혔다.
  국제수지 표가 계층 구조라 항목명 규칙이 다른 듯하다.

둘 다 "외국인 자금" 축을 노린 것이었는데, **외국인 순매수가 일간으로 있어**
페어 드롭다운이 그 역할을 더 잘한다(§2-A).

### 미국 금리는 ECOS 에 없다

`902Y023` 주요국제금리의 미국 항목은 **연·월·분기뿐**이다(`USA`, 195304~202608).
일간이 없으므로 미 10년물은 **FRED `DGS10` 를 계속 쓴다**(키 불필요, `Accept` 헤더 필요).

### 호출량

시계열은 **시리즈당 1콜**이다. 페어 1개 + 카드 11장이면 하루 12콜 남짓이라 한도를
걱정할 규모가 아니다. `KeyStatisticList` 는 1콜로 101개 지표의 **최신값**을 주지만
스파크라인에는 시계열이 필요하므로 개별 호출이 맞다.

## 5. ⚠️ 함정 — 착수 전에 반드시 읽을 것

### A. `pctChg` 가 월간 데이터에서 조용히 틀린다

`dashboard.js:31`:

```js
function pctChg(v) { var back = v[Math.max(0, v.length - 22)]; ... }
```

`-22` 는 **거래일 22일 ≈ 1개월**을 노린 값이다(섹터 카드는 일간 주가라 맞다).
월간 시리즈에 그대로 쓰면 **22개월 전 대비**가 계산되고, 라벨은
`per = period || "~1M"` 이라 **"~1M" 으로 표시된다**.
CPI 처럼 단조 상승하는 지표면 `+18.3% ~1M` 이 찍혀도 아무도 이상하게 보지 않는다.

### B. `manual` 플래그가 두 일을 겸하고 있다

`dashboard.js:97`:

```js
var chg = (manual && v.length >= 2) ? (v[len-1]/v[len-2] - 1)*100 : pctChg(v);
```

`manual=true` 는 **직전 대비 계산**과 **`<span class="vc-tag">수동</span>` 배지**를
동시에 켠다. 월간 지표는 앞은 필요하고 뒤는 틀리다(수동 입력이 아니다).
**플래그를 분리해야 한다** — §2-C 의 `mode` 필드가 그 자리다.

### C. 투자자예탁금은 일간이 아니다

`802Y001`(주식시장, 일간)에 예탁금은 없다. 100대 지표에는 월간(202608)으로 나온다.
대신 **외국인 순매수가 일간**이라 전달 경로를 더 잘 보여준다 — §2-A 드롭다운에 넣었다.
예탁금을 카드로 쓰려면 표를 따로 찾아야 한다.

### D. `.env` 파싱 시 주석을 자를 것

`BoKorea/.env` 는 `API_KEY=... # 주석` 형태다. `split("=",1)[1]` 만 하면 주석이 딸려와
URL 에 공백이 들어가고 요청이 깨진다. `re.split(r'[#\s]', v.strip())[0]` 로 자른다.

> 2026-09-06 에 이 실수로 오류 메시지에 키 값이 노출됐다. **재발급 권장.**

## 6. 배치와 보안

- 키는 GitHub Secret **`BOK_API_KEY`** 로 등록한다(BSL Secrets 16번째).
  수집은 `fetch_dashboard.py` 가 Actions 에서 돌므로 로컬 `.env` 로는 운영이 안 된다.
- `BoKorea/.env` 는 로컬 개발용으로만 남는다. **`BoKorea/` 폴더 자체는 임시 자리**이고,
  실제 코드는 전부 `BrieSigLab/` 안에서 바뀐다.
- ECOS 는 읽기 전용 공개 통계라 키 유출의 피해 범위는 제한적이다. 그래도 재발급한다.

## 7. 착수 시 손댈 곳

```
scripts/fetch_dashboard.py   ECOS 소스 함수 + 스프레드 계산(교집합) + 시리즈 등록
public/dashboard.html        <section class="band" id="kr-macro"> 1개 추가
public/assets/dashboard.js   vcCard 에 mode 분기 + kr-macro 호스트 렌더
public/assets/content.js     i18n 문자열(ko/en) — 카드 11장 + 섹션 제목
tests/                       회귀 잠금 (아래 §8)
```

새 렌더러는 만들지 않는다. `vcCard()` 를 확장한다.

## 8. 남은 것

```
✔ 항목 코드 — 전부 확정, 실제 호출로 검증 (§4)
✔ 소매 총지수 — 901Y098 에 없어 901Y100/G0 로 확정
✔ 히스토리 — 일간 RANGE(3년) · 월간 KR_MACRO_MONTHS=72(6년). 월간은 74포인트라 부담 없음
✔ 영문 라벨 — 섹션 제목·설명 en 추가(카드 이름은 섹터와 같이 JSON 의 한글을 그대로 쓴다)
✔ 회귀 테스트 — test_dashboard_pairs.py(EcosTests 10건) + test_dashboard_card_modes.js

□ GitHub Secret BOK_API_KEY 등록 — 안 하면 Actions 에서 한국 매크로만 조용히 빠진다
  (워크플로 env 배선은 끝났고 test_workflow_passes_the_bok_key 가 잠갔다)
□ ECOS 갱신 시각 — 하루 중 언제 새 값이 들어오는지 모른다. 지금은 대시보드 크론
  (평일 06:30 KST)에 얹혀 있고, D-2 데이터라 문제된 적은 없다
□ 허위상관 검증 — 추세 제거 후 상관 재측정 (§3-C). 수치를 문구로 쓸 때만 필요
□ 모바일 세로 스크롤 — 카드가 10 + 11(섹터) + 5(밸류체인) = 26장이 됐다.
  실제 기기에서 한 번 볼 것. 정적 검사로는 증명되지 않는다
□ 투자자예탁금 · 증권투자 — 시계열 경로를 못 찾았다 (§4 '찾지 못한 것')
```

## 9. 결정 이력

| 결정 | 근거 |
|---|---|
| 만기 10년−10년 | 만기 대응, 기간프리미엄 미혼입, 히스토리 깊음 |
| 우축 KOSPI | 실측 상관이 원/달러보다 강하고 `dollar-kospi` 와 안 겹침 (§3-C) |
| 드롭다운 ECOS 통일 | `fetch_dashboard.py:56` 규칙을 정면 만족 |
| 교집합, forward-fill 없음 | 94.3% 보존이라 보간이 불필요 (§3-B) |
| 월간은 카드 | 신선도 기준은 차트 대상. 카드+기준시점 라벨이면 해당 없음 |
| 표시 기준 지표별 | 통일하면 CPI 나 심리지수 중 하나가 오독됨 (§2-C) |
| 카드 11장 | 4개 묶음 전부 채택. 소제목이 붙으면 섹터 11장과 같은 형태 |
